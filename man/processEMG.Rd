% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mocap-emg.R
\name{processEMG}
\alias{processEMG}
\title{Process EMG for biomechanics workflows}
\usage{
processEMG(
  x,
  sampling_rate,
  bandpass = c(20, 450),
  envelope_cutoff = 6,
  rms_window_ms = 50,
  mvc = NULL,
  filter_method = c("butterworth", "moving_average")
)
}
\arguments{
\item{x}{Numeric vector or matrix (time x channels).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{bandpass}{Optional length-2 numeric vector (Hz). If \code{NULL}, no
band-pass filtering is applied.}

\item{envelope_cutoff}{Low-pass cutoff (Hz) applied to RMS envelope.}

\item{rms_window_ms}{RMS window length in milliseconds.}

\item{mvc}{Optional MVC value(s) for normalization.}

\item{filter_method}{Filter method used in low-pass steps.}
}
\value{
A list with \code{filtered}, \code{rectified}, \code{envelope}, and
(when \code{mvc} is provided) \code{normalized}.
}
\description{
Applies optional band-pass filtering, rectification, RMS envelope
extraction, optional low-pass smoothing, and optional MVC normalization.
}
\examples{
set.seed(1)
emg <- matrix(rnorm(2000), ncol = 2)
out <- processEMG(emg, sampling_rate = 1000)
}
\references{
Merletti R, Parker PA (2004). "Electromyography: Physiology, Engineering,
and Non-Invasive Applications." IEEE Press/Wiley.
}
\seealso{
\code{\link[=rectifyEMG]{rectifyEMG()}} for signal rectification,
\code{\link[=computeRMSEnvelope]{computeRMSEnvelope()}} for RMS envelope computation,
\code{\link[=normalizeEMG]{normalizeEMG()}} for MVC or peak normalization,
\code{\link[=integrateEMGMoCap]{integrateEMGMoCap()}} for EMG-MoCap data integration.
}
