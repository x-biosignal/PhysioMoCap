% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-forceplate.R
\name{analyzeForcePlatePE}
\alias{analyzeForcePlatePE}
\title{Analyze force-plate signals from a PhysioExperiment object}
\usage{
analyzeForcePlatePE(
  pe,
  force_assay = "grf",
  moment_assay = NULL,
  plate_index = 1L,
  sampling_rate = NULL,
  ...
)
}
\arguments{
\item{pe}{A PhysioExperiment object.}

\item{force_assay}{Assay name containing force data with 3 columns
(X, Y, Z). If not present, the function tries \code{force_x}, \code{force_y},
and \code{force_z} assays.}

\item{moment_assay}{Optional assay name containing moments with 3 columns.
If not present, the function tries \code{moment_x}, \code{moment_y}, and \code{moment_z}.}

\item{plate_index}{Which plate to analyze when components are provided as
separate assays with multiple force plates. Accepts:
\itemize{
\item numeric scalar (specific plate),
\item \code{"auto"} (select plate with largest vertical-force impulse),
\item \code{"all"} (analyze all plates and return a multi-plate result).
}}

\item{sampling_rate}{Optional sampling rate in Hz. If \code{NULL}, uses
\code{samplingRate(pe)}.}

\item{...}{Additional arguments passed to \code{analyzeForcePlate()}.}
}
\value{
A \code{forceplate_analysis} object (single plate) or
\code{forceplate_analysis_multi} (when \code{plate_index = "all"}).
}
\description{
Convenience wrapper for \code{analyzeForcePlate()} when force and moment
components are stored in PhysioExperiment assays.
}
\examples{
\dontrun{
pe <- readC3D("trial.c3d", include_analog = TRUE)
out <- analyzeForcePlatePE(pe)
}
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=analyzeForcePlate]{analyzeForcePlate()}} for matrix-based force plate analysis,
\code{\link[=print.forceplate_analysis]{print.forceplate_analysis()}} for displaying results.
}
