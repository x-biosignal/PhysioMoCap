% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-reliability.R
\name{waveformICC}
\alias{waveformICC}
\title{Pointwise intraclass correlation across the movement cycle}
\usage{
waveformICC(x, model = "twoway", type = "agreement", unit = "single")
}
\arguments{
\item{x}{A \verb{[subject, trial, frame]} array, or a list of \verb{trials x frames}
matrices (one per subject). Every subject must have the same trials and
frames.}

\item{model, type, unit}{Passed to \code{\link[PhysioCore:icc]{PhysioCore::icc()}} (default two-way,
absolute agreement, single measure -- ICC(2,1)).}
}
\value{
a \code{waveform_icc} list: \code{icc}, \code{ci_lower}, \code{ci_upper} (per frame),
\code{mean_icc}, \code{min_icc}.
}
\description{
The reliability \emph{curve}: the between-subject ICC computed frame-by-frame from
repeated trials, using the ecosystem's \code{\link[PhysioCore:icc]{PhysioCore::icc()}} at each cycle point.
It shows where along the movement a measurement is reliable.
}
\examples{
set.seed(1)
subs <- lapply(1:8, function(s) {
  base <- s + sin(seq(0, 2 * pi, length.out = 101))   # subject offset = signal
  t(sapply(1:3, function(k) base + rnorm(101, 0, 0.1)))
})
waveformICC(subs)$mean_icc                            # high (subjects separable)
}
\seealso{
\code{\link[=waveformReliability]{waveformReliability()}}, \code{\link[=waveformCMC]{waveformCMC()}}
}
