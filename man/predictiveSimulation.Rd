% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-forward-dynamics.R
\name{predictiveSimulation}
\alias{predictiveSimulation}
\title{Predictive simulation: minimal-effort optimal control}
\usage{
predictiveSimulation(
  model,
  q0,
  qT,
  duration,
  n_nodes = 60L,
  n_basis = 3L,
  maxit = 200L
)
}
\arguments{
\item{model}{A \code{planar_limb_model}.}

\item{q0, qT}{Start and target joint configurations (length \code{ndof}); velocity
is zero at both ends.}

\item{duration}{Movement time (s).}

\item{n_nodes}{Collocation nodes for the effort integral (default 60).}

\item{n_basis}{Interior basis functions per joint (default 3).}

\item{maxit}{Optimiser iterations (default 200).}
}
\value{
a \code{predictive_sim}: \code{time}, \code{q}, \code{qd}, \code{qdd}, \code{tau} (node x ndof),
\code{effort} (optimised) and \code{effort_baseline} (the minimum-jerk quintic), the
effort reduction, and boundary error.
}
\description{
Predicts the movement a limb model would produce to go from one posture to
another while minimising control effort (integral of squared joint torque) --
a direct-collocation optimal-control problem. Each joint trajectory is a
boundary-satisfying quintic plus interior basis functions whose amplitudes are
optimised; the required torque at each node is obtained by inverse dynamics.
}
\examples{
m <- planarLimbModel(c(2.1, 1.4), c(0.3, 0.25), g = 0)   # horizontal reach
ps <- predictiveSimulation(m, q0 = c(0.2, 0.3), qT = c(1.1, 0.6),
                           duration = 0.6, n_basis = 2, maxit = 80)
ps$effort <= ps$effort_baseline                          # optimisation helps
}
\references{
Todorov & Jordan (2002); Ackermann & van den Bogert (2010)
predictive simulation.
}
\seealso{
\code{\link[=forwardDynamics]{forwardDynamics()}}
}
