% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imu-fusion.R
\name{mahonyAHRS}
\alias{mahonyAHRS}
\title{Mahony AHRS orientation filter}
\usage{
mahonyAHRS(gyro, accel, sampling_rate, kp = 1, ki = 0.1)
}
\arguments{
\item{gyro}{\verb{n x 3} angular velocity (rad/s), columns x, y, z.}

\item{accel}{\verb{n x 3} accelerometer (any consistent unit; normalised
internally).}

\item{sampling_rate}{Sampling rate (Hz).}

\item{kp, ki}{Proportional and integral feedback gains (default 1, 0.1).}
}
\value{
a data frame \verb{time, roll, pitch, yaw} (rad) and \verb{q_w, q_x, q_y, q_z},
matching \code{\link[=estimateOrientation]{estimateOrientation()}}.
}
\description{
Estimates orientation by integrating the gyroscope while correcting drift with
the accelerometer (gravity) and optional magnetometer through a
proportional+integral complementary feedback on SO(3) (Mahony et al. 2008).
}
\examples{
n <- 200; sr <- 100; t <- seq_len(n) / sr
roll <- 0.3 * sin(2 * pi * 0.5 * t); pitch <- 0.2 * cos(2 * pi * 0.3 * t)
accel <- cbind(-sin(pitch), sin(roll) * cos(pitch), cos(roll) * cos(pitch))
gyro <- cbind(c(0, diff(roll)) * sr, c(0, diff(pitch)) * sr, 0)
est <- mahonyAHRS(gyro, accel, sampling_rate = sr)
max(abs(est$roll - roll))
}
\references{
Mahony R, et al. (2008) IEEE Trans Autom Control 53:1203-1218.
}
\seealso{
\code{\link[=kalmanOrientation]{kalmanOrientation()}}, \code{\link[=estimateOrientation]{estimateOrientation()}}
}
