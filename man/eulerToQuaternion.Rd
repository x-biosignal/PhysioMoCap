% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mocap-angles.R
\name{eulerToQuaternion}
\alias{eulerToQuaternion}
\title{Convert Euler angles to quaternion}
\usage{
eulerToQuaternion(roll, pitch, yaw, order = "ZYX")
}
\arguments{
\item{roll}{Numeric. Rotation about X axis (in radians by default,
or degrees if values > 2*pi suggest degree input).}

\item{pitch}{Numeric. Rotation about Y axis.}

\item{yaw}{Numeric. Rotation about Z axis.}

\item{order}{Character. Rotation order. Default \code{"ZYX"}.}
}
\value{
A matrix with columns \code{w}, \code{x}, \code{y}, \code{z}. If inputs are
scalars, returns a 1-row matrix. If inputs are vectors, returns
a matrix with one row per element.
}
\description{
Converts Euler angles (roll, pitch, yaw) to quaternion (w, x, y, z)
representation using the specified rotation order.
}
\details{
Input angles are assumed to be in \strong{radians}. For the \code{"ZYX"}
convention, the combined quaternion is computed as:
\eqn{q = q_z \otimes q_y \otimes q_x}

The resulting quaternion is always returned with \eqn{w \geq 0}
(canonical form).
}
\examples{
# Zero rotation -> identity quaternion
eulerToQuaternion(0, 0, 0)

# 90-degree rotation about Z axis (input in radians)
eulerToQuaternion(0, 0, pi/2)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. Wiley.

Grood ES, Suntay WJ (1983). "A joint coordinate system for the clinical
description of three-dimensional motions: application to the knee."
Journal of Biomechanical Engineering, 105(2), 136-144.
}
\seealso{
\code{\link[=quaternionToEuler]{quaternionToEuler()}}, \code{\link[=calculateJointAngles]{calculateJointAngles()}}, \code{\link[=vectorAngle]{vectorAngle()}}
}
