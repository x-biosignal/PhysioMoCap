% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opensim-integration.R
\name{runStaticOptimization}
\alias{runStaticOptimization}
\title{Run static optimization (OpenSim, or a pure-R fallback)}
\usage{
runStaticOptimization(
  so_setup = NULL,
  moment_arms = NULL,
  max_force = NULL,
  joint_moments = NULL,
  execution = c("auto", "opensim", "r"),
  workdir = NULL,
  cli = NULL,
  timeout_sec = 0L,
  fail_on_error = TRUE,
  ...
)
}
\arguments{
\item{so_setup}{Path to an OpenSim Static Optimization setup XML (OpenSim
backend).}

\item{moment_arms, max_force, joint_moments}{Inputs for the pure-R fallback,
passed to \code{\link[=staticOptimizationR]{staticOptimizationR()}}.}

\item{execution}{Backend selection: \code{"auto"} (default; OpenSim when available
and \code{so_setup} is given, else pure-R), \code{"opensim"}, or \code{"r"}.}

\item{workdir, cli, timeout_sec, fail_on_error}{Passed to
\code{PhysioOpenSim::opensimRunSO()} for the OpenSim backend.}

\item{...}{Additional arguments passed to \code{\link[=staticOptimizationR]{staticOptimizationR()}} for the
pure-R backend (e.g. \code{weights}, \code{activation_bounds}).}
}
\value{
An \code{opensim_so_result} list with \code{backend} (\code{"opensim"} or \code{"r"}) and
either \code{tool} (the PhysioOpenSim tool result) or \code{static_optimization} (the
\code{\link[=staticOptimizationR]{staticOptimizationR()}} result).
}
\description{
Resolves net joint moments into muscle activations. When an OpenSim backend
is available and a Static Optimization setup file is supplied, it runs the
OpenSim Static Optimization tool via PhysioOpenSim; otherwise it falls back to
\code{\link[=staticOptimizationR]{staticOptimizationR()}}, the pure-R quadratic muscle-effort solver, using the
supplied moment arms, maximum forces and joint moments.
}
\seealso{
\code{\link[=staticOptimizationR]{staticOptimizationR()}}, \code{\link[=runRRA]{runRRA()}}, \code{\link[=runCMC]{runCMC()}}
}
