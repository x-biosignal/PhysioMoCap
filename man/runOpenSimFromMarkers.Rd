% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opensim-integration.R
\name{runOpenSimFromMarkers}
\alias{runOpenSimFromMarkers}
\title{Run the local OpenSim toolchain from a model and marker file}
\usage{
runOpenSimFromMarkers(
  model_file,
  trc_file,
  tools = "ik",
  external_loads_file = NULL,
  time_range = NULL,
  workdir = NULL,
  templates = NULL,
  cli = NULL,
  require_external_loads = TRUE,
  dry_run = FALSE
)
}
\arguments{
\item{model_file}{Path to a scaled OpenSim \code{.osim} model (e.g. from
\code{\link[=downloadOpenCapModel]{downloadOpenCapModel()}}).}

\item{trc_file}{Path to a marker trajectory \code{.trc} file.}

\item{tools}{Which tools to run, a subset of \code{"ik"}, \code{"id"}, \code{"so"} (IK is
always required and run first). Default \code{"ik"}.}

\item{external_loads_file}{Path to an OpenSim \code{ExternalLoads} XML (ground
reactions). Required for \code{"id"}/\code{"so"} unless \code{require_external_loads} is
\code{FALSE}.}

\item{time_range}{Optional numeric \code{c(start, end)} (s) applied to every tool.
If \code{NULL} (default) the whole trial is used, read from \code{trc_file} (otherwise
the tool templates' own \verb{<time_range>} would truncate the trial to 0-1 s).}

\item{workdir}{Directory for setup XMLs and outputs (a temporary directory by
default).}

\item{templates}{Optional named list of template paths (\code{ik}/\code{id}/\code{so});
defaults to the templates bundled with \pkg{PhysioOpenSim}.}

\item{cli}{Optional path to the \code{opensim-cmd} CLI (see \code{\link[=run_opensim_toolchain]{run_opensim_toolchain()}}).}

\item{require_external_loads}{If \code{TRUE} (default) error when \code{"id"}/\code{"so"}
are requested without \code{external_loads_file}.}

\item{dry_run}{If \code{TRUE}, only write the setup XMLs and return them without a
working OpenSim backend (useful for inspection/testing). Default \code{FALSE}.}
}
\value{
An \code{opensim_run} list: \code{setups} (written XML paths), \code{workdir},
\code{expected_outputs}; and, when run, \code{toolchain} (raw \code{\link[=run_opensim_toolchain]{run_opensim_toolchain()}}
result), \code{files} (produced \code{.mot}/\code{.sto}), \code{outputs} (their parsed
\code{PhysioExperiment} objects from \code{\link[=readOpenSimOutputs]{readOpenSimOutputs()}}), \code{motion} (the IK
joint-angle object), and \code{backend}.
}
\description{
Convenience wrapper that wires markers through OpenSim inverse kinematics
(IK) and, optionally, inverse dynamics (ID) and static optimization (SO): it
writes the tool setup XMLs from the bundled \pkg{PhysioOpenSim} templates,
runs them in order via \code{\link[=run_opensim_toolchain]{run_opensim_toolchain()}}, and parses the results with
\code{\link[=readOpenSimOutputs]{readOpenSimOutputs()}} into a downstream-ready form. This is the local
counterpart to using OpenCap's cloud kinematics directly.
}
\details{
IK needs only the scaled model and markers. \strong{ID and SO additionally need
ground-reaction data} (\code{external_loads_file}); markerless OpenCap has no
force plates, so you must supply a measured or estimated \code{ExternalLoads} XML
(or set \code{require_external_loads = FALSE} to run kinematics-only inverse
dynamics, which is valid only for non-contact phases such as swing). CMC and
RRA need actuator/tracking-task files beyond what this wrapper infers; run
them via \code{\link[=run_opensim_toolchain]{run_opensim_toolchain()}} with the corresponding
\code{PhysioOpenSim::opensimWrite*SetupFromTemplate()} writers.
}
\examples{
\dontrun{
res <- runOpenSimFromMarkers(
  model_file = "subject_scaled.osim", trc_file = "walk.trc",
  tools = c("ik", "id", "so"), external_loads_file = "walk_grf.xml")
res$motion            # IK joint angles (PhysioExperiment)
calculateJointAngles(res$motion)
}
}
\seealso{
\code{\link[=runOpenSimFromOpenCap]{runOpenSimFromOpenCap()}}, \code{\link[=run_opensim_toolchain]{run_opensim_toolchain()}},
\code{\link[=readOpenSimOutputs]{readOpenSimOutputs()}}, \code{\link[=downloadOpenCapModel]{downloadOpenCapModel()}}
}
