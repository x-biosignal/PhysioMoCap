% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-opensim.R
\name{writeTRC}
\alias{writeTRC}
\title{Write an OpenSim TRC File (.trc)}
\usage{
writeTRC(x, path)
}
\arguments{
\item{x}{A \code{PhysioExperiment} with \code{"position_x"}, \code{"position_y"}, and
\code{"position_z"} assays (as produced by \code{\link[=readTRC]{readTRC()}} or \code{\link[=readC3D]{readC3D()}}).}

\item{path}{Character string giving the output \code{.trc} path.}
}
\value{
The output \code{path}, invisibly.
}
\description{
Writes 3D marker trajectories from a \code{PhysioExperiment} to an OpenSim \code{.trc}
file. This is the inverse of \code{\link[=readTRC]{readTRC()}}: a
\code{readTRC() -> writeTRC() -> readTRC()} round-trip reproduces the marker
coordinates exactly and the \code{DataRate} header field.
}
\details{
Marker labels, units (\code{metadata(x)$Units}, default \code{"mm"}), frame rate, and
the time column (\code{metadata(x)$time}, else derived from \code{samplingRate(x)})
are preserved on write.
}
\examples{
trc_file <- system.file("testdata", "sample.trc", package = "PhysioMoCap")
if (nzchar(trc_file)) {
  pe <- readTRC(trc_file)
  out <- tempfile(fileext = ".trc")
  writeTRC(pe, out)
}
}
\seealso{
\code{\link[=readTRC]{readTRC()}}, \code{\link[=writeMOT]{writeMOT()}}, \code{\link[=writeC3D]{writeC3D()}}
}
