% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-forceplate.R
\name{calculateCOP}
\alias{calculateCOP}
\title{Calculate center of pressure (COP) from force-plate data}
\usage{
calculateCOP(forces, moments, origin = c(0, 0, 0), min_vertical_force = 20)
}
\arguments{
\item{forces}{Matrix/data.frame with at least three columns for force
components (X, Y, Z).}

\item{moments}{Matrix/data.frame with at least three columns for moment
components (X, Y, Z).}

\item{origin}{Numeric length-3 vector with force-plate origin \verb{(x, y, z)}.}

\item{min_vertical_force}{Minimum absolute vertical force required to compute
COP (samples below this threshold are set to \code{NA}).}
}
\value{
A data.frame with columns \code{cop_x}, \code{cop_y}, \code{cop_z},
\code{free_moment}, and \code{vertical_force}.
}
\description{
Computes COP coordinates from force and moment components using standard
force-plate equations (assuming a vertical Z axis).
}
\examples{
f <- cbind(Fx = rep(0, 10), Fy = rep(0, 10), Fz = rep(1000, 10))
m <- cbind(Mx = rep(100, 10), My = rep(-200, 10), Mz = rep(0, 10))
cop <- calculateCOP(f, m)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=analyzeForcePlate]{analyzeForcePlate()}} for comprehensive force plate analysis,
\code{\link[=calculateCOM]{calculateCOM()}} for whole-body center of mass computation.
}
