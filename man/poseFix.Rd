% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pose-fix.R
\name{poseFix}
\alias{poseFix}
\title{Detect and correct pose-estimation anomalies}
\usage{
poseFix(
  pe,
  conf_threshold = 0.2,
  length_tol = 0.4,
  jump_tol = 0.5,
  angle_range = c(0.01, 0.99),
  skeleton = NULL,
  joints = NULL,
  deswap = TRUE,
  length_correct = FALSE,
  smooth = TRUE,
  smooth_spar = 0.4
)
}
\arguments{
\item{pe}{A \code{PhysioExperiment} of pose keypoints with assays \code{keypoint_x},
\code{keypoint_y} and (optionally) \code{confidence}, and \code{colData$label} naming the
keypoints — the output of \code{\link[=readOpenPose]{readOpenPose()}} / \code{\link[=readMediaPipe]{readMediaPipe()}}.}

\item{conf_threshold}{Keypoints with confidence below this are flagged
(default 0.2).}

\item{length_tol}{A bone is flagged when its length deviates from its own
median by more than this fraction (default 0.4).}

\item{jump_tol}{A keypoint is flagged when its frame-to-frame displacement
exceeds this fraction of body scale (default 0.5).}

\item{angle_range}{Lower/upper quantiles bounding plausible joint angles
(default \code{c(0.01, 0.99)}).}

\item{skeleton}{Optional list of \code{c(from, to)} keypoint-name bone pairs
(default: a standard body skeleton restricted to the present keypoints).}

\item{joints}{Optional list of \code{c(a, b, c)} angle triplets (angle at \code{b}).}

\item{deswap}{If \code{TRUE} (default), first correct left/right leg-label swaps by
restoring trajectory continuity (a common pose-estimation error during gait).}

\item{length_correct}{If \code{TRUE}, additionally standardise segment lengths with
\code{\link[=poseLengthCorrect]{poseLengthCorrect()}} after cleaning (camera-distance correction; default
\code{FALSE}).}

\item{smooth}{If \code{TRUE} (default) smooth each coordinate with a spline after
interpolation.}

\item{smooth_spar}{Smoothing parameter passed to \code{\link[stats:smooth.spline]{stats::smooth.spline()}}.}
}
\value{
A cleaned \code{PhysioExperiment} (corrected \code{keypoint_x}/\code{keypoint_y}
assays); \code{metadata()$poseFix} holds the per-criterion anomaly counts, the
number of leg-swap corrections (\code{leg_swaps}), the \code{flagged} frame-by-keypoint
logical matrix, and the flagged fraction.
}
\description{
Cleans a markerless-pose recording (e.g. from \code{\link[=readOpenPose]{readOpenPose()}}) by detecting
biomechanically implausible keypoints and correcting them, connecting pose
estimation to the downstream joint-angle / gait / kinematics analysis.
Anomalies are flagged from four criteria (after Sugiyama, Uno & Matsui 2023):
low detector confidence; segment (bone) lengths that deviate from the
subject's own median; frame-to-frame jumps larger than a fraction of body
scale; and joint angles outside their empirical range. Flagged coordinates are
set to \code{NA}, linearly interpolated, and optionally smoothed. References are
derived from the recording itself, so the method is model-agnostic.
}
\references{
Sugiyama S, Uno K, Matsui Y (2023). Types of anomalies in
two-dimensional video-based gait analysis in uncontrolled environments.
\emph{PLOS Computational Biology} 19:e1009989.
\doi{10.1371/journal.pcbi.1009989}
}
\seealso{
\code{\link[=readOpenPose]{readOpenPose()}}, \code{\link[=calculateJointAngles]{calculateJointAngles()}}
}
