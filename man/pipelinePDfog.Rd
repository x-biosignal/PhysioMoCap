% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines-population.R
\name{pipelinePDfog}
\alias{pipelinePDfog}
\title{Parkinson's freezing-of-gait pipeline (freeze index)}
\usage{
pipelinePDfog(
  accel,
  sampling_rate,
  window_sec = 4,
  step_sec = 0.5,
  freeze_band = c(3, 8),
  locomotor_band = c(0.5, 3),
  fi_threshold = 2,
  power_threshold = NULL
)
}
\arguments{
\item{accel}{Accelerometer signal (e.g. shank/trunk vertical or resultant).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{window_sec, step_sec}{Sliding-window length and step (seconds).}

\item{freeze_band, locomotor_band}{Frequency bands (Hz).}

\item{fi_threshold}{Freeze-index threshold for FOG (default 2).}

\item{power_threshold}{Minimum band power for a window to be considered
(excludes rest). \code{NULL} uses the 10th percentile of windowed band power as a
quiet-baseline floor (robust to a loud walking bout, unlike a
maximum-relative gate); pass a fixed absolute value if the sensor units are
known.}
}
\value{
A \code{pd_fog_report} object with a per-window \code{windows} data frame
(\code{time}, \code{freeze_index}, \code{power}, \code{fog}) and the overall \code{fog_fraction}.
}
\description{
Detects freezing of gait (FOG) from a windowed spectral freeze index: the
ratio of power in the freeze band (default 3-8 Hz) to power in the locomotor
band (default 0.5-3 Hz) of an accelerometer signal (Moore et al. 2008;
Bachlin et al. 2010). A window is flagged as FOG when the freeze index exceeds
\code{fi_threshold} and the band power exceeds \code{power_threshold} (excluding rest).
}
\references{
Moore ST, et al. (2008); Bachlin M, et al. (2010).
}
