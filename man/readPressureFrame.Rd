% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plantar-pressure.R
\name{readPressureFrame}
\alias{readPressureFrame}
\title{Read a plantar-pressure ASCII export}
\usage{
readPressureFrame(
  file,
  sampling_rate = NULL,
  dx = NULL,
  dy = NULL,
  side = NA_character_,
  units = "kPa",
  heel_first = TRUE,
  frame_marker = "^\\\\s*(Frame|ASCII_DATA|Time)",
  comment = "#"
)
}
\arguments{
\item{file}{Path to an ASCII pressure export.}

\item{sampling_rate, dx, dy}{Optional overrides for file metadata.}

\item{side}{Optional side override.}

\item{units}{Pressure-unit override. When omitted, file metadata is used,
falling back to \code{"kPa"}.}

\item{heel_first}{Logical; whether row 1 represents the heel.}

\item{frame_marker}{Regular expression identifying frame-header lines.}

\item{comment}{Literal prefix identifying metadata/comment lines.}
}
\value{
A \code{pressure_movie} object.
}
\description{
Reads frame-delimited Novel/Tekscan-style text exports. Comment metadata
may define \code{sampling_rate} (or \code{fs}), \code{dx}, \code{dy}, \code{side}, and \code{units}.
Explicit function arguments take precedence over file metadata.
}
\examples{
f <- tempfile(fileext = ".txt")
writeLines(c("# sampling_rate 100", "Frame 0", "1 2", "3 4"), f)
readPressureFrame(f)
unlink(f)
}
