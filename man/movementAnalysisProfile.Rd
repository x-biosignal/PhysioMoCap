% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gait-indices.R
\name{movementAnalysisProfile}
\alias{movementAnalysisProfile}
\title{Movement Analysis Profile (GVS per variable plus the GPS)}
\usage{
movementAnalysisProfile(kinematics, norm = NULL)
}
\arguments{
\item{kinematics}{Subject kinematics: a numeric matrix with one row per
kinematic variable (row names matching \code{norm$variables}) and one column per
gait-cycle point, or a named list of per-variable curves. Curves are
time-normalised to the norm's cycle length via \code{\link[=normalizeMovement]{normalizeMovement()}}.}

\item{norm}{A \code{gait_norm} normative reference (from
\code{PhysioGaitNorm::loadGaitNorm()}); if \code{NULL}, the default reference is
loaded from PhysioGaitNorm.}
}
\value{
A \code{movement_analysis_profile} object: a list with \code{gvs} (named
numeric vector), \code{gps} (numeric), \code{variables}, and \code{cycle_length}.
}
\description{
The Movement Analysis Profile (Baker et al. 2009) collects the per-variable
Gait Variable Scores together with the overall Gait Profile Score.
}
\examples{
norm <- list(variables = c("a", "b"),
             mean = matrix(0, 2, 51, dimnames = list(c("a", "b"), NULL)),
             cycle_length = 51)
movementAnalysisProfile(matrix(1, 2, 51, dimnames = list(c("a", "b"), NULL)),
                        norm)
}
\references{
Baker R, et al. (2009). Gait & Posture 30(3):265-269.
}
\seealso{
\code{\link[=gaitProfileScore]{gaitProfileScore()}}, \code{\link[=gaitVariableScore]{gaitVariableScore()}}, \code{\link[=plotMAP]{plotMAP()}}
}
