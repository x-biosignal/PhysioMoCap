% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-opensim.R
\name{writeMOT}
\alias{writeMOT}
\title{Write an OpenSim Motion File (.mot)}
\usage{
writeMOT(x, path)
}
\arguments{
\item{x}{A \code{PhysioExperiment} whose first assay holds the data columns.}

\item{path}{Character string giving the output \code{.mot} path.}
}
\value{
The output \code{path}, invisibly.
}
\description{
Writes tabular time-series data (joint angles, kinematics, ...) from a
\code{PhysioExperiment} to an OpenSim \code{.mot} file. This is the inverse of
\code{\link[=readMOT]{readMOT()}}: a \code{readMOT() -> writeMOT() -> readMOT()} round-trip reproduces
every numeric column exactly and the derived sampling rate.
}
\details{
The time column is taken from \code{metadata(x)$time} when present, otherwise it
is reconstructed from \code{samplingRate(x)}. The \code{inDegrees} header flag is
preserved from \code{metadata(x)$inDegrees} (default \code{"yes"}).
}
\examples{
mot_file <- system.file("testdata", "sample.mot", package = "PhysioMoCap")
if (nzchar(mot_file)) {
  pe <- readMOT(mot_file)
  out <- tempfile(fileext = ".mot")
  writeMOT(pe, out)
}
}
\seealso{
\code{\link[=readMOT]{readMOT()}}, \code{\link[=writeTRC]{writeTRC()}}
}
