% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipelines-population.R
\name{pipelineACLrts}
\alias{pipelineACLrts}
\title{ACL return-to-sport pipeline (limb symmetry index)}
\usage{
pipelineACLrts(tests, threshold = 90)
}
\arguments{
\item{tests}{A data frame with columns \code{test}, \code{involved}, \code{uninvolved}, and
optionally \code{higher_better} (logical, default \code{TRUE}); or a named list of
\code{c(involved, uninvolved)} pairs.}

\item{threshold}{LSI percentage required to pass each test (default 90).}
}
\value{
An \code{acl_rts_report} object with a per-test \code{lsi} data frame and the
overall \code{rts_ready} flag.
}
\description{
Computes the Limb Symmetry Index (LSI) for a battery of functional tests and
evaluates the return-to-sport (RTS) criterion that every LSI meets a threshold
(default 90\\%; Grindem et al. 2016). LSI is \code{involved / uninvolved * 100} for
tests where a higher score is better, and inverted otherwise.
}
\references{
Grindem H, et al. (2016). Br J Sports Med 50(13):804-808.
}
