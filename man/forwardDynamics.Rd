% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-forward-dynamics.R
\name{forwardDynamics}
\alias{forwardDynamics}
\title{Forward dynamics: integrate the equations of motion}
\usage{
forwardDynamics(model, q0, qd0, torque = NULL, dt = 0.001, n_steps = 1000L)
}
\arguments{
\item{model}{A \code{planar_limb_model} (or any list with a \code{forward_accel(q, qd, tau)} and \code{ndof}).}

\item{q0, qd0}{Initial joint angles and velocities (length \code{ndof}).}

\item{torque}{Joint torques: \code{NULL} (free/passive), a length-\code{ndof} constant,
an \verb{n_steps x ndof} matrix, or a function \verb{(t, q, qd) -> torque}.}

\item{dt}{Time step (s).}

\item{n_steps}{Number of integration steps.}
}
\value{
an \code{fd_result}: \code{time}, \code{q}, \code{qd} (\verb{(n_steps+1) x ndof}), \code{tau}, and
\code{energy} (total mechanical energy per step).
}
\description{
Given a limb model, initial state and joint torques, integrates the motion
with a fixed-step 4th-order Runge-Kutta scheme.
}
\examples{
m <- planarLimbModel(c(7, 3), c(0.4, 0.4))
fd <- forwardDynamics(m, q0 = c(pi/2 - 0.3, 0.2), qd0 = c(0, 0),
                      torque = NULL, dt = 0.001, n_steps = 500)
diff(range(fd$energy)) / mean(abs(fd$energy))     # ~ 0: energy conserved
}
\seealso{
\code{\link[=planarLimbModel]{planarLimbModel()}}, \code{\link[=predictiveSimulation]{predictiveSimulation()}}
}
