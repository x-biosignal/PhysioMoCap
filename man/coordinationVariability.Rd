% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordination.R
\name{coordinationVariability}
\alias{coordinationVariability}
\title{Inter-joint coordination variability across cycles}
\usage{
coordinationVariability(
  angle1_cycles,
  angle2_cycles,
  method = c("crp", "vector_coding")
)
}
\arguments{
\item{angle1_cycles, angle2_cycles}{Numeric matrices \verb{cycles x points} (each
row one time-normalised cycle) of the two joints' angles.}

\item{method}{\code{"crp"} (deviation phase, linear SD) or \code{"vector_coding"}
(coupling-angle circular SD).}
}
\value{
a \code{coordination_variability} list: \code{method}, \code{variability} (per-point),
\code{mean_variability} and \code{measure} (the per-cycle coordination matrix).
}
\description{
The trial-to-trial (cycle-to-cycle) variability of the coordination measure --
the deviation phase for continuous relative phase (linear SD) or the coupling-
angle circular SD for vector coding -- computed at each point of the
(time-normalised) cycle. Coordination variability is a motor-control marker:
abnormally high or low values accompany injury and neurological disease.
}
\examples{
m <- matrix(sin(seq(0, 2 * pi, length.out = 50)), nrow = 5, ncol = 50,
            byrow = TRUE)
coordinationVariability(m, m)$mean_variability      # identical cycles -> 0
}
\references{
Hamill (2000); Needham (2014).
}
\seealso{
\code{\link[=continuousRelativePhase]{continuousRelativePhase()}}, \code{\link[=vectorCoding]{vectorCoding()}}
}
