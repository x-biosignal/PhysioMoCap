% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imu-fusion.R
\name{kalmanOrientation}
\alias{kalmanOrientation}
\title{Kalman orientation filter (tilt + gyro-bias)}
\usage{
kalmanOrientation(
  gyro,
  accel,
  sampling_rate,
  mag = NULL,
  q_angle = 0.001,
  q_bias = 0.003,
  r_measure = 0.03
)
}
\arguments{
\item{gyro}{\verb{n x 3} angular velocity (rad/s).}

\item{accel}{\verb{n x 3} accelerometer.}

\item{sampling_rate}{Sampling rate (Hz).}

\item{mag}{Optional \verb{n x 3} magnetometer for yaw.}

\item{q_angle, q_bias}{Process-noise variances for angle and bias (defaults
1e-3, 3e-3).}

\item{r_measure}{Measurement-noise variance of the accelerometer angle
(default 0.03).}
}
\value{
a data frame \verb{time, roll, pitch, yaw} (rad) and quaternion columns,
plus attribute \code{bias} (estimated gyro bias per axis).
}
\description{
Fuses gyroscope and accelerometer with a per-axis linear Kalman filter that
estimates both the tilt angle and the (slowly varying) gyroscope bias -- the
classic optimal complementary filter. Roll and pitch are corrected by gravity;
yaw is integrated (or corrected by a magnetometer heading if supplied).
}
\examples{
n <- 300; sr <- 100; t <- seq_len(n) / sr
roll <- 0.4 * sin(2 * pi * 0.4 * t); pitch <- 0.3 * sin(2 * pi * 0.25 * t)
accel <- cbind(-sin(pitch), sin(roll) * cos(pitch), cos(roll) * cos(pitch))
gyro <- cbind(c(0, diff(roll)) * sr + 0.05, c(0, diff(pitch)) * sr - 0.03, 0)
est <- kalmanOrientation(gyro, accel, sampling_rate = sr)
max(abs(est$roll - roll))
}
\references{
Lauszus K (2012) Kalman IMU; Brown & Hwang (1997).
}
\seealso{
\code{\link[=mahonyAHRS]{mahonyAHRS()}}, \code{\link[=estimateOrientation]{estimateOrientation()}}
}
