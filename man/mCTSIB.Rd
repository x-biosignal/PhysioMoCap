% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posturography.R
\name{mCTSIB}
\alias{mCTSIB}
\title{Modified Clinical Test of Sensory Interaction on Balance (mCTSIB)}
\usage{
mCTSIB(
  eyes_open_firm = NULL,
  eyes_closed_firm = NULL,
  eyes_open_foam = NULL,
  eyes_closed_foam = NULL,
  conditions = NULL
)
}
\arguments{
\item{eyes_open_firm, eyes_closed_firm, eyes_open_foam, eyes_closed_foam}{Numeric vectors of per-trial sway velocities (or another sway index) for
each condition. Missing conditions may be \code{NULL}.}

\item{conditions}{Alternatively, a named list with those four elements.}
}
\value{
An \code{mctsib_result} object with \code{condition_means}, \code{condition_sd},
\code{n_trials} and the \code{composite} mean.
}
\description{
Summarises sway velocity (or any sway index) across the four mCTSIB sensory
conditions (eyes open / closed on a firm surface and on foam) and returns the
per-condition means and the composite (mean of the four condition means).
}
\examples{
mCTSIB(eyes_open_firm = c(0.4, 0.5),
       eyes_closed_firm = c(0.6, 0.7),
       eyes_open_foam = c(0.9, 1.0),
       eyes_closed_foam = c(1.6, 1.8))
}
\references{
Cohen H, Blatchly CA, Gombash LL (1993). Phys Ther 73(6):346-351.
}
\seealso{
\code{\link[=sensoryOrganizationTest]{sensoryOrganizationTest()}}, \code{\link[=swayMetrics]{swayMetrics()}}
}
