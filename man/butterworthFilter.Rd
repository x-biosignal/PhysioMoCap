% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-filter.R
\name{butterworthFilter}
\alias{butterworthFilter}
\title{Apply a Butterworth filter to numeric data}
\usage{
butterworthFilter(
  x,
  cutoff,
  sampling_rate,
  type = c("lowpass", "highpass", "bandpass", "bandstop"),
  order = 4
)
}
\arguments{
\item{x}{A numeric vector or matrix (time x channels).}

\item{cutoff}{Cutoff frequency in Hz. A single value for "lowpass" or
"highpass"; a length-2 vector \code{c(low, high)} for "bandpass" or "bandstop".}

\item{sampling_rate}{Sampling rate in Hz.}

\item{type}{Filter type: "lowpass", "highpass", "bandpass", or "bandstop".}

\item{order}{Filter order (default: 4).}
}
\value{
Filtered data with the same dimensions as \code{x}.
}
\description{
Designs a Butterworth IIR filter and applies it using zero-phase filtering
(forward-backward) to avoid phase distortion.
}
\details{
Requires the \pkg{signal} package. NAs in the input are temporarily
interpolated before filtering and restored afterward.
}
\examples{
\dontrun{
# Filter a single vector
x <- sin(2 * pi * 5 * seq(0, 1, length.out = 1000)) +
     sin(2 * pi * 50 * seq(0, 1, length.out = 1000))
x_filt <- butterworthFilter(x, cutoff = 20, sampling_rate = 1000, type = "lowpass")
}
}
\references{
Butterworth S (1930). "On the Theory of Filter Amplifiers."
Wireless Engineer, 7, 536-541.
}
\seealso{
\code{\link[=filterSignals]{filterSignals()}} for filtering PhysioExperiment objects,
\code{\link[=savgolFilter]{savgolFilter()}} for Savitzky-Golay polynomial smoothing.
}
