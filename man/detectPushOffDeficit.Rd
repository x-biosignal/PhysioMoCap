% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gait-pathology.R
\name{detectPushOffDeficit}
\alias{detectPushOffDeficit}
\title{Detect push-off deficit (reduced ankle A2 power / plantarflexor output)}
\usage{
detectPushOffDeficit(
  ankle_power,
  window = c(40, 62),
  normal_peak = 3.5,
  threshold = 2,
  metric_name = "peak_ankle_A2_power"
)
}
\arguments{
\item{ankle_power}{Ankle joint power over one gait cycle. Interpreted as
W/kg (or the peak ankle plantarflexor moment if \code{metric_name} is set).}

\item{window}{Percentage window of late stance / push-off
(default \code{c(40, 62)}).}

\item{normal_peak}{Normal peak A2 power (default 3.5 W/kg).}

\item{threshold}{Peak below which a deficit is flagged (default 2.0).}

\item{metric_name}{Label for the metric (default \code{"peak_ankle_A2_power"}).}
}
\value{
A \code{gait_pattern_flag} object.
}
\description{
Reduced ankle plantarflexor push-off is quantified from the peak positive
ankle power in late stance (the A2 burst; normal \eqn{\approx 3-4} W/kg) or,
equivalently, the peak late-stance plantarflexor moment.
}
\references{
Perry J, Burnfield JM (2010); Winter DA (2009).
}
\seealso{
\code{\link[=classifyGaitPatterns]{classifyGaitPatterns()}}
}
