% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-filter.R
\name{filterSignals}
\alias{filterSignals}
\title{Filter signals in a PhysioExperiment object}
\usage{
filterSignals(
  pe,
  type = c("lowpass", "highpass", "bandpass", "bandstop"),
  cutoff,
  order = 4,
  assay_name = NULL,
  output_assay = NULL
)
}
\arguments{
\item{pe}{A PhysioExperiment object.}

\item{type}{Filter type: "lowpass", "highpass", "bandpass", or "bandstop".}

\item{cutoff}{Cutoff frequency in Hz. A single value for "lowpass" or
"highpass"; a length-2 vector \code{c(low, high)} for "bandpass" or "bandstop".}

\item{order}{Filter order (default: 4).}

\item{assay_name}{Which assay to filter. If NULL, uses the first assay.}

\item{output_assay}{Name for the output assay. If NULL, defaults to
\code{"{assay_name}_filtered"}.}
}
\value{
A PhysioExperiment object with filtered data stored as a new assay.
}
\description{
Applies a Butterworth IIR filter to signal data stored in a
PhysioExperiment object. The filter is applied using zero-phase filtering
(forward-backward) to avoid phase distortion.
}
\details{
This function requires the \pkg{signal} package (listed in Suggests).
If not installed, an informative error message with install instructions
is provided.

The Butterworth filter is designed using \code{\link[signal:butter]{signal::butter()}} and applied
with \code{\link[signal:filtfilt]{signal::filtfilt()}} for zero-phase filtering. The cutoff frequency
is normalized to the Nyquist frequency automatically.
}
\examples{
\dontrun{
pe <- make_mocap_markers(n_time = 500, n_markers = 4, sr = 120)
pe_filt <- filterSignals(pe, type = "lowpass", cutoff = 10)
}
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.

Butterworth S (1930). "On the Theory of Filter Amplifiers."
Wireless Engineer, 7, 536-541.
}
\seealso{
\code{\link[=butterworthFilter]{butterworthFilter()}} for filtering raw vectors and matrices,
\code{\link[=savgolFilter]{savgolFilter()}} for Savitzky-Golay polynomial smoothing,
\code{\link[=movingAverage]{movingAverage()}} for simple moving average smoothing.
}
