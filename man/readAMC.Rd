% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-asfamc.R
\name{readAMC}
\alias{readAMC}
\title{Read AMC Motion Capture File (.amc)}
\usage{
readAMC(path, asf = NULL, fps = 120)
}
\arguments{
\item{path}{Character string giving the path to the \code{.amc} file.}

\item{asf}{An \code{ASFSkeleton} object (from \code{\link[=readASF]{readASF()}}), or \code{NULL}. If
provided, used for DOF validation and enriched metadata.}

\item{fps}{Numeric frame rate in Hz. AMC files do not store frame rate, so
this must be specified (default: 120).}
}
\value{
A \code{PhysioExperiment} object with:
\describe{
\item{assays}{\code{rotation_x}, \code{rotation_y}, \code{rotation_z} for all joints;
\code{position_x}, \code{position_y}, \code{position_z} for the root joint only.}
\item{colData}{DataFrame with \code{label} (joint names), \code{type}
(\code{"root"} or \code{"joint"}), and \code{dof_count} (number of DOFs).}
\item{metadata}{List with \code{asf_skeleton} (if provided), \code{source_file},
and \code{units} (from ASF if provided).}
\item{samplingRate}{Set to \code{fps}.}
}
}
\description{
Parses an Acclaim Motion Capture (AMC) file containing frame-by-frame joint
angle data. AMC files store joint angles per frame, with each frame listing
joint names followed by their DOF values. An optional ASF skeleton can be
provided for validation and DOF mapping.
}
\examples{
asf_file <- system.file("testdata", "sample.asf", package = "PhysioMoCap")
amc_file <- system.file("testdata", "sample.amc", package = "PhysioMoCap")
if (nzchar(asf_file) && nzchar(amc_file)) {
  skel <- readASF(asf_file)
  pe <- readAMC(amc_file, asf = skel)
  pe
}
}
\references{
CMU Graphics Lab (2003). "CMU Motion Capture Database."
\url{http://mocap.cs.cmu.edu/}.
}
\seealso{
\code{\link[=readASF]{readASF()}} for reading skeleton definitions in ASF format,
\code{\link[=readMoCapCSV]{readMoCapCSV()}} for reading motion capture data from CSV files.
}
