% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posturography.R
\name{swayMetrics}
\alias{swayMetrics}
\title{Posturographic center-of-pressure sway metrics (Prieto 1996)}
\usage{
swayMetrics(
  cop,
  sampling_rate,
  ap = NULL,
  ml = NULL,
  detrend = c("mean", "none"),
  entropy = TRUE,
  m = 2L,
  r = 0.2,
  base_of_support = NULL
)
}
\arguments{
\item{cop}{Center-of-pressure data. A \code{data.frame} from \code{\link[=calculateCOP]{calculateCOP()}}
(columns \code{cop_x} = ML, \code{cop_y} = AP), a two-column matrix/\code{data.frame}
(columns interpreted as \code{ml}, \code{ap} unless named), or \code{NULL} when supplying
\code{ap}/\code{ml} directly.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{ap, ml}{Optional anteroposterior / mediolateral CoP vectors (override
\code{cop}).}

\item{detrend}{Either \code{"mean"} (subtract the mean CoP, the Prieto
convention) or \code{"none"}.}

\item{entropy}{Logical; compute per-axis \code{\link[=sampleEntropy]{sampleEntropy()}} and DFA-\eqn{\alpha}
(default \code{TRUE}).}

\item{m, r}{Sample-entropy embedding dimension and tolerance (tolerance is a
fraction of each axis SD).}

\item{base_of_support}{Optional numeric \code{c(ap = , ml = )} giving the half
extent of the base of support (in CoP units) about the mean CoP, used for
time-to-boundary. \code{NULL} (default) returns \code{NA} for the TTB measures.}
}
\value{
A \code{sway_metrics} object (a named list) with the Prieto measures and,
in \verb{$metrics}, a one-row \code{data.frame} aligned to the \link{schema_balance}
metric names.
}
\description{
Computes the full Prieto et al. (1996) battery of postural-steadiness
measures from a center-of-pressure (CoP) time series, together with the
non-linear complexity descriptors used by \link{schema_balance}. Distances are
reported in the units of the supplied CoP (typically mm or m); frequencies in
Hz; areas in CoP-units squared; velocities in CoP-units per second.
}
\details{
The measures are defined relative to the mean CoP. With \eqn{RD_n} the
resultant distance of sample \eqn{n} from the mean CoP and \eqn{T} the trial
duration:
\itemize{
\item \strong{Path length} (total excursion, TOTEX): the summed point-to-point
distance; \strong{mean velocity} is TOTEX / T.
\item \strong{RMS distance} (RDIST) and \strong{mean distance} (MDIST), resultant and
per-axis.
\item \strong{95\% confidence circle area} (AREA-CC)
\eqn{= \pi (MDIST + z\, s_{RD})^2} with \eqn{z = 1.645}.
\item \strong{95\% confidence ellipse area} (AREA-CE)
\eqn{= 2\pi F_{0.05[2,n-2]} \sqrt{s_{AP}^2 s_{ML}^2 - s_{AP,ML}^2}}.
\item \strong{Sway area} (AREA-SW), area swept per unit time.
\item \strong{Mean frequency} (MFREQ), the equivalent rotational frequency
\eqn{MVELO / (2\pi\, MDIST)}.
\item \strong{Fractal dimension} (FD-CC) and frequency-domain measures (total power,
50\%/95\% power frequency, centroidal frequency, frequency dispersion).
\item \strong{Non-linear} sample entropy and DFA-\eqn{\alpha} per axis, and optional
time-to-boundary if a base of support is supplied.
}
}
\examples{
t <- seq(0, 30, by = 0.01)
cop <- data.frame(cop_x = 2 * sin(2 * pi * 0.2 * t),
                  cop_y = 3 * cos(2 * pi * 0.2 * t))
sm <- swayMetrics(cop, sampling_rate = 100)
sm$path_length
sm$mean_velocity
}
\references{
Prieto TE, Myklebust JB, Hoffmann RG, Lovett EG, Myklebust BM (1996).
"Measures of postural steadiness." IEEE Trans Biomed Eng 43(9):956-966.
}
\seealso{
\code{\link[=calculateCOP]{calculateCOP()}}, \code{\link[=analyzeForcePlate]{analyzeForcePlate()}}, \code{\link[=stabilogramDiffusion]{stabilogramDiffusion()}},
\code{\link[=sensoryOrganizationTest]{sensoryOrganizationTest()}}, \link{schema_balance}
}
