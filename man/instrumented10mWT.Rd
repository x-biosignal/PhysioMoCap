% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical-mobility-tests.R
\name{instrumented10mWT}
\alias{instrumented10mWT}
\title{Instrumented 10-metre walk test (10mWT)}
\usage{
instrumented10mWT(
  position,
  sampling_rate,
  total_distance = 10,
  mid_start = 2,
  mid_end = 8,
  events = NULL,
  events_unit = c("auto", "index", "seconds")
)
}
\arguments{
\item{position}{Cumulative forward distance walked (m) over time.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{total_distance}{Total walkway length in m (default 10).}

\item{mid_start, mid_end}{Start/end of the timed steady-state section in m
(defaults 2 and 8).}

\item{events}{Optional heel-strike sample indices or times (s) for cadence.}

\item{events_unit}{Unit of \code{events}: \code{"auto"} (default) guesses indices vs
seconds, or force \code{"index"} / \code{"seconds"}. Pass an explicit unit when
heel-strike times are whole-number seconds (which \code{"auto"} would otherwise
read as sample indices).}
}
\value{
A \code{walk_test_report} object with \code{gait_speed} (m/s), \code{mid_distance},
\code{mid_time}, \code{cadence_spm} (or \code{NA}), and the section boundary times.
}
\description{
Extracts steady-state gait speed (and cadence) over the middle section of a
10-metre walk, excluding the acceleration and deceleration zones, from the
forward walking distance over time (e.g. the ZUPT-integrated foot trajectory
from \code{\link[=footImuGait]{footImuGait()}} or a marker distance).
}
\references{
Standard 10-metre walk test protocol.
}
\seealso{
\code{\link[=instrumentedTUG]{instrumentedTUG()}}, \code{\link[=footImuGait]{footImuGait()}}
}
