% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-dimred.R
\name{extractWaveformFeatures}
\alias{extractWaveformFeatures}
\title{Dimensionality Reduction for Biomechanics}
\usage{
extractWaveformFeatures(x, features = c("statistical", "shape"), n_points = 50)
}
\arguments{
\item{x}{A PhysioExperiment object or matrix (time x observations).}

\item{features}{Character vector of features to extract:
"raw" (flattened waveform), "statistical" (summary stats),
"frequency" (spectral features), "shape" (curve characteristics).}

\item{n_points}{For "raw", number of points to resample to.}
}
\value{
A matrix (observations x features) suitable for PCA/UMAP.
}
\description{
Functions for dimensionality reduction and visualization of high-dimensional
biomechanical waveform data including PCA, UMAP, and t-SNE.
Extract waveform features for dimensionality reduction
}
\details{
Extracts summary features from waveforms for use with standard
dimensionality reduction methods.

Feature types:
\itemize{
\item raw: The raw waveform resampled to n_points
\item statistical: Mean, SD, min, max, range, skewness, kurtosis
\item frequency: Dominant frequency, spectral centroid, bandwidth
\item shape: Peaks, zero crossings, area under curve
}
}
\examples{
# Extract features from gait data
set.seed(123)
data <- matrix(rnorm(1000), nrow = 100, ncol = 10)
features <- extractWaveformFeatures(data, features = c("statistical", "shape"))
}
\references{
van der Maaten L, Hinton G (2008). "Visualizing Data using t-SNE."
Journal of Machine Learning Research, 9, 2579-2605.
}
\seealso{
\code{\link[=waveformPCA]{waveformPCA()}}, \code{\link[=waveformUMAP]{waveformUMAP()}}, \code{\link[=waveformTSNE]{waveformTSNE()}}
}
