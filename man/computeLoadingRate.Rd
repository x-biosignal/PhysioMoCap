% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-forceplate.R
\name{computeLoadingRate}
\alias{computeLoadingRate}
\title{Compute vertical loading rate from GRF}
\usage{
computeLoadingRate(
  vgrf,
  sampling_rate,
  threshold = 20,
  method = c("instantaneous", "average")
)
}
\arguments{
\item{vgrf}{Numeric vector or matrix of vertical GRF values.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{threshold}{Contact threshold in force units.}

\item{method}{Loading-rate method: \code{"instantaneous"} (max slope) or
\code{"average"} (peak rise over time-to-peak).}
}
\value{
For vector input: a data.frame with one row per stance.
For matrix input: same data.frame with an added \code{channel} column.
}
\description{
Detects stance phases from a vertical GRF signal and computes loading rate
from initial contact to peak force for each stance.
}
\examples{
grf <- c(rep(0, 100), seq(0, 800, length.out = 150),
         seq(800, 0, length.out = 150), rep(0, 100))
computeLoadingRate(grf, sampling_rate = 1000)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=analyzeForcePlate]{analyzeForcePlate()}} for comprehensive force plate analysis,
\code{\link[=computeImpulse]{computeImpulse()}} for impulse computation.
}
