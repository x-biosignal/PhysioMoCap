% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-circular.R
\name{circularLinearCorrelation}
\alias{circularLinearCorrelation}
\title{Mardia's circular-linear correlation}
\usage{
circularLinearCorrelation(theta, x, units = c("degrees", "radians"))
}
\arguments{
\item{theta}{Numeric angular variable.}

\item{x}{Numeric linear variable (same length as \code{theta}).}

\item{units}{\code{"degrees"} (default) or \code{"radians"} for \code{theta}.}
}
\value{
an \code{htest}: the circular-linear correlation \code{r} (\code{estimate}), the test
statistic \code{n * r^2 ~ chi-square(2)} and its \code{p.value}.
}
\description{
The association between an angular variable (e.g. a coupling angle or relative
phase) and a linear one (e.g. speed, force, a clinical score). Symmetric under
rotation of the angle.
}
\examples{
set.seed(3); th <- runif(60, 0, 360); y <- cos(th * pi / 180) + rnorm(60, 0, 0.3)
circularLinearCorrelation(th, y)$estimate            # strong association
}
\references{
Mardia KV (1976); Zar JH (1999) eq. 27.19.
}
\seealso{
\code{\link[=circularSummary]{circularSummary()}}
}
