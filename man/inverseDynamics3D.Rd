% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-inverse-dynamics.R
\name{inverseDynamics3D}
\alias{inverseDynamics3D}
\title{Compute 3D lower-limb joint moments with inverse dynamics}
\usage{
inverseDynamics3D(
  joints,
  grf,
  sampling_rate,
  angles = NULL,
  angular_velocity = NULL,
  angular_acceleration = NULL,
  inertial = NULL,
  angle_unit = c("radian", "degree"),
  model = c("newton_euler", "quasi_static"),
  body_mass = NULL,
  body_height = NULL,
  gravity = .GRAVITY,
  vertical = c("y", "z")
)
}
\arguments{
\item{joints}{Matrix/data.frame with columns \code{ankle_x}, \code{ankle_y}, \code{ankle_z},
\code{knee_x}, \code{knee_y}, \code{knee_z}, \code{hip_x}, \code{hip_y}, \code{hip_z}, and - for
\code{model = "newton_euler"} - the foot distal end \code{toe_x}, \code{toe_y}, \code{toe_z}.}

\item{grf}{Matrix/data.frame with columns \code{fx}, \code{fy}, \code{fz} and optional
\code{cop_x}, \code{cop_y}, \code{cop_z} (and, for \code{model = "newton_euler"}, optional free
moments \code{tx}, \code{ty}, \code{tz}).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{angles}{Optional matrix/data.frame containing 3D joint angles with
columns \code{ankle_x}, \code{ankle_y}, \code{ankle_z}, \code{knee_x}, \code{knee_y}, \code{knee_z},
\code{hip_x}, \code{hip_y}, \code{hip_z}.}

\item{angular_velocity}{Optional 3D joint angular velocity table.}

\item{angular_acceleration}{Optional 3D joint angular acceleration table.}

\item{inertial}{Optional data.frame from \code{estimateSegmentInertia()}. Required
for \code{model = "newton_euler"} unless \code{body_mass} is given; under
\code{model = "quasi_static"} it only supplies an \code{I * alpha} correction and is
ignored when omitted.}

\item{angle_unit}{Unit of angle-related inputs: \code{"radian"} or \code{"degree"}.}

\item{model}{Which dynamics model to use: \code{"newton_euler"} (the default,
the full recursive chain via \code{\link[=inverseDynamicsRNE]{inverseDynamicsRNE()}}) or \code{"quasi_static"}
(the legacy massless-segment approximation; see \code{\link[=inverseDynamics2D]{inverseDynamics2D()}}).}

\item{body_mass}{Body mass in kg, used to build the segment inertia table
with \code{\link[=estimateSegmentInertia]{estimateSegmentInertia()}} when \code{inertial} is not supplied.}

\item{body_height}{Body height in m, used when segment lengths must be
estimated from stature rather than measured from the markers.}

\item{gravity}{Gravitational acceleration in m/s^2.}

\item{vertical}{Which coordinate axis points up, \code{"y"} (default) or \code{"z"}.
Gravity acts along the negative of this axis, so it must match the
laboratory convention of \code{joints}. The default follows this package's
marker convention (x antero-posterior, y vertical, z medio-lateral) and
the sagittal \code{inverseDynamics2D()}; note that force-plate hardware
normally reports the vertical force as \code{fz}, so \code{grf} may need reordering
to match. A mismatch is checked against the marker geometry and warned
about, because the wrong axis silently removes every gravitational moment.
Used only by \code{model = "newton_euler"}; the quasi-static model has no
gravity term and therefore ignores it.}
}
\value{
A data.frame with time and moment components:
\verb{*_moment_x}, \verb{*_moment_y}, \verb{*_moment_z}. If angular velocity is
available, \verb{*_power_total} columns are included.
\code{model = "newton_euler"} also returns the proximal joint reaction force
components (\code{ankle_fx}, \code{ankle_fy}, \code{ankle_fz}, ...).
}
\description{
Computes ankle, knee, and hip net moment vectors in 3D from joint-center
coordinates, GRF, COP, and segment inertial properties, using a recursive
link-segment Newton-Euler chain (foot -> shank -> thigh).
}
\details{
See \code{\link[=inverseDynamics2D]{inverseDynamics2D()}} for what the two models compute and why the
quasi-static one is retained only for reproducibility. The 3D moment balance
uses an axisymmetric segment inertia and neglects the gyroscopic
\eqn{\omega \times I \omega} term and any spin about the segment long axis,
which is not observable from two joint centres.
}
\examples{
n <- 200
joints <- data.frame(
  ankle_x = rep(0.00, n), ankle_y = rep(0.05, n), ankle_z = rep(0.00, n),
  toe_x   = rep(0.15, n), toe_y   = rep(0.01, n), toe_z   = rep(0.00, n),
  knee_x = rep(0.00, n),  knee_y = rep(0.45, n),  knee_z = rep(0.00, n),
  hip_x = rep(0.00, n),   hip_y = rep(0.85, n),   hip_z = rep(0.00, n)
)
grf <- data.frame(
  fx = rep(50, n), fy = rep(700, n), fz = rep(0, n),
  cop_x = rep(0.02, n), cop_y = rep(0, n), cop_z = rep(0, n)
)
out <- inverseDynamics3D(joints, grf, sampling_rate = 100, body_mass = 70)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=inverseDynamicsRNE]{inverseDynamicsRNE()}} for the underlying recursion,
\code{\link[=inverseDynamics2D]{inverseDynamics2D()}} for sagittal-plane inverse dynamics,
\code{\link[=estimateSegmentInertia]{estimateSegmentInertia()}} for segment inertial properties,
\code{\link[=computeJointPower]{computeJointPower()}} for joint power calculation.
}
