% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posturography.R
\name{limitsOfStability}
\alias{limitsOfStability}
\title{Limits of Stability (LOS) reaction time, excursion and directional control}
\usage{
limitsOfStability(
  cop,
  sampling_rate,
  target,
  ap = NULL,
  ml = NULL,
  onset_cue = 1L,
  onset_frac = 0.05,
  los_distance = NULL
)
}
\arguments{
\item{cop, ap, ml}{CoP trace, as in \code{\link[=swayMetrics]{swayMetrics()}} (a single leaning trial).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{target}{Target direction: an angle in degrees (0 = +AP/forward,
90 = +ML/right, measured counter-clockwise from forward) or a length-2
numeric \code{c(ap, ml)} direction vector.}

\item{onset_cue}{Sample index of the go cue (default 1).}

\item{onset_frac}{Fraction of the maximum excursion defining movement onset
(default 0.05).}

\item{los_distance}{Optional theoretical limit-of-stability distance (CoP
units) for the target; if supplied, excursions are also returned as a
percentage of it.}
}
\value{
A \code{los_result} object with \code{reaction_time}, \code{endpoint_excursion},
\code{max_excursion}, optional \code{endpoint_pct} / \code{max_pct}, and
\code{directional_control}.
}
\description{
Quantifies a single limits-of-stability leaning trial toward a target
direction from a CoP trace: reaction time, endpoint and maximum excursion
(optionally as a percentage of a theoretical limit), and directional control.
}
\details{
The CoP is projected onto the unit vector toward the target. Reaction time is
the delay from the go cue to movement onset (projection first exceeding
\code{onset_frac} of the maximum excursion). Endpoint excursion is the projection
at the end of the initial movement (its first local maximum after onset);
maximum excursion is the largest projection. Directional control is
\eqn{100 \times} the path travelled toward the target divided by the total
path length.
}
\examples{
t <- seq(0, 3, by = 0.01)
lean <- pmin(t / 1.5, 1) * 5             # ramp forward to 5 units
cop <- data.frame(cop_x = rnorm(length(t), 0, 0.02), cop_y = lean)
limitsOfStability(cop, sampling_rate = 100, target = 0)
}
\references{
Nashner LM (1997). "Computerized dynamic posturography."
}
\seealso{
\code{\link[=sensoryOrganizationTest]{sensoryOrganizationTest()}}, \code{\link[=swayMetrics]{swayMetrics()}}
}
