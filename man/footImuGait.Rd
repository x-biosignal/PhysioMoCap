% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imu-gait.R
\name{footImuGait}
\alias{footImuGait}
\title{Foot-IMU spatiotemporal gait parameters}
\usage{
footImuGait(
  accel,
  gyro,
  sampling_rate,
  g = 9.81,
  orientation = NULL,
  orientation_method = c("madgwick", "complementary"),
  beta = 0.02,
  stance = NULL,
  vertical_axis = 3,
  ...
)
}
\arguments{
\item{accel}{Numeric matrix (n x 3) of accelerometer readings (m/s^2).}

\item{gyro}{Numeric matrix (n x 3) of gyroscope readings (rad/s).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{g}{Gravitational acceleration magnitude (default 9.81 m/s^2).}

\item{orientation}{Optional orientation (a data frame with \code{q_w}/\code{q_x}/\code{q_y}/
\code{q_z}, or an n x 4 quaternion matrix). If \code{NULL}, \code{\link[=estimateOrientation]{estimateOrientation()}}
is run internally.}

\item{orientation_method}{Sensor-fusion method for the internal orientation
estimate, \code{"madgwick"} (default) or \code{"complementary"}. Ignored when
\code{orientation} is supplied.}

\item{beta}{Madgwick filter gain for the internal orientation estimate
(default 0.02). A foot IMU sees large dynamic accelerations, so a small,
gyro-dominant gain avoids swing-phase tilt error; raise it for
lower-dynamic mounting or noisier gyroscopes. Ignored when \code{orientation}
is supplied.}

\item{stance}{Optional logical stance vector; if \code{NULL}, \code{\link[=detectStanceZUPT]{detectStanceZUPT()}}
is used.}

\item{vertical_axis}{World axis that points up (1, 2 or 3; default 3). Used
for foot clearance and to de-drift vertical position to the floor.}

\item{...}{Additional arguments forwarded to \code{\link[=detectStanceZUPT]{detectStanceZUPT()}} (e.g.
\code{method}, \code{accel_threshold}).}
}
\value{
An \code{imu_gait} object: a list with \code{strides} (a data frame with one
row per detected stride: \code{stride}, \code{stride_length}, \code{foot_clearance},
\code{stance_time}, \code{swing_time}, \code{stride_time}, \code{gait_velocity}), the world-frame
\code{position} and \code{velocity} matrices, the \code{stance} mask, and \code{sampling_rate}.
}
\description{
End-to-end foot-mounted IMU gait pipeline: estimate orientation, remove
gravity, rotate acceleration into the world frame, detect stance, and
ZUPT-integrate to a drift-bounded foot trajectory, then derive per-stride
stride length, foot clearance, stance/swing time and gait velocity.
}
\references{
Mariani B et al. (2010) J Biomech 43(15):2999-3006;
Rebula JR, Ojeda LV, Adamczyk PG, Kuo AD (2013). "Measurement of foot
placement and its variability with inertial sensors." Gait & Posture
38(4):974-980.
}
\seealso{
\code{\link[=detectStanceZUPT]{detectStanceZUPT()}}, \code{\link[=strapdownIntegrate]{strapdownIntegrate()}},
\code{\link[=estimateOrientation]{estimateOrientation()}}, \code{\link[=removeGravity]{removeGravity()}}.
}
