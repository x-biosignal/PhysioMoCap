% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomech-com.R
\name{calculateSegmentCOM}
\alias{calculateSegmentCOM}
\title{Calculate per-segment centers of mass}
\usage{
calculateSegmentCOM(pe, proximal_markers, distal_markers, com_fractions)
}
\arguments{
\item{pe}{A \code{PhysioExperiment} object with \code{position_x}, \code{position_y},
and optionally \code{position_z} assays.}

\item{proximal_markers}{Character vector of proximal marker names,
one per segment.}

\item{distal_markers}{Character vector of distal marker names,
one per segment (same length as \code{proximal_markers}).}

\item{com_fractions}{Numeric vector of COM proximal fractions (0-1),
one per segment (same length as \code{proximal_markers}).}
}
\value{
A named list of matrices, one per segment. Each matrix has
dimensions (n_frames x 2) for 2D data or (n_frames x 3) for 3D
data, with columns named \code{"x"}, \code{"y"}, and (optionally) \code{"z"}.
}
\description{
Lower-level function that computes the center of mass for individual
body segments from marker position data.
}
\examples{
\dontrun{
pe <- readTRC("markers.trc")
seg_coms <- calculateSegmentCOM(
  pe,
  proximal_markers = c("RHip", "LHip"),
  distal_markers   = c("RKnee", "LKnee"),
  com_fractions    = c(0.4095, 0.4095)
)
}
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=calculateCOM]{calculateCOM()}} for whole-body center of mass,
\code{\link[=segmentParameters]{segmentParameters()}} for body segment inertial parameters.
}
