% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events-zeni.R
\name{detectEventsZeni}
\alias{detectEventsZeni}
\title{Detect gait events with the Zeni coordinate method}
\usage{
detectEventsZeni(
  pe,
  markers,
  reference = "sacrum",
  ap_axis = NULL,
  direction = NULL,
  body_mass = NULL,
  min_separation = NULL,
  sampling_rate = NULL
)
}
\arguments{
\item{pe}{A \code{PhysioExperiment} with marker \code{position_x}/\code{position_y}
(and optionally \code{position_z}) assays (frames x markers).}

\item{markers}{A named list of marker names (columns of the position assays)
with entries \code{heel_left}, \code{heel_right}, \code{toe_left}, \code{toe_right}. A side is
skipped if both its heel and toe entries are \code{NULL}/missing.}

\item{reference}{Progression reference: a marker name (e.g. a sacral marker,
the default \code{"sacrum"}), or \code{"com"} to use the whole-body centre of mass
from \code{\link[=calculateCOM]{calculateCOM()}} (requires \code{body_mass}).}

\item{ap_axis}{Anterior-posterior (progression) axis: \code{"x"}, \code{"y"}, \code{"z"}
or \code{1}/\code{2}/\code{3}. \code{NULL} (default) auto-detects the axis with the largest
heel-relative-to-reference excursion.}

\item{direction}{\code{+1} or \code{-1} to orient the AP axis so anterior is positive;
\code{NULL} (default) auto-detects it (the fastest relative foot motion, i.e.
swing, points anterior).}

\item{body_mass}{Body mass in kg, required only when \code{reference = "com"}.}

\item{min_separation}{Minimum number of frames between successive same-type
events (refractory period). \code{NULL} (default) estimates it from the dominant
stride period via autocorrelation.}

\item{sampling_rate}{Sampling rate in Hz; defaults to \code{samplingRate(pe)}.}
}
\value{
A \code{detected_events} data frame with one row per detected event
(columns \code{event}, \code{label}, \code{index}, \code{time}, \code{percent}, \code{method},
\code{confidence}, \code{side}), sorted by frame index.
}
\description{
Implements the coordinate-based gait-event detector of Zeni, Richards &
Higginson (2008). Heel strike (initial contact, IC) occurs when a heel
marker reaches its most anterior position relative to a pelvis/COM reference;
toe off (TO) occurs when a toe marker reaches its most posterior position
relative to the same reference. Because events are keyed to the foot marker
\emph{relative to} the pelvis, the method works on treadmill trials where the
body does not translate in the lab frame, and it returns per-stride IC/TO for
both sides across a full trial.
}
\details{
The result is a \code{detected_events} data frame compatible with
\code{\link[=calculateGaitParameters]{calculateGaitParameters()}}; heel-strike/toe-off rows are named
\code{left_heel_strike}/\code{right_heel_strike}/\code{left_toe_off}/\code{right_toe_off} and
carry an explicit \code{side} column.
}
\references{
Zeni JA, Richards JG, Higginson JS (2008). "Two simple methods for
determining gait events during treadmill and overground walking."
Gait & Posture 27(4):710-714.
}
\seealso{
\code{\link[=detectEvents]{detectEvents()}} (use \code{method = "zeni"}),
\code{\link[=calculateGaitParameters]{calculateGaitParameters()}}, \code{\link[=calculateCOM]{calculateCOM()}}.
}
