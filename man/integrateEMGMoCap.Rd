% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mocap-emg.R
\name{integrateEMGMoCap}
\alias{integrateEMGMoCap}
\title{Integrate EMG and MoCap signals onto a common timeline}
\usage{
integrateEMGMoCap(
  mocap,
  emg,
  mocap_sampling_rate = NULL,
  emg_sampling_rate,
  mocap_assay = NULL,
  process = TRUE,
  ...
)
}
\arguments{
\item{mocap}{A numeric matrix/data.frame (time x features), or a
PhysioExperiment object.}

\item{emg}{Numeric vector or matrix (time x channels).}

\item{mocap_sampling_rate}{MoCap sampling rate in Hz. If \code{mocap} is a
PhysioExperiment and this is \code{NULL}, uses \code{samplingRate(mocap)}.}

\item{emg_sampling_rate}{EMG sampling rate in Hz.}

\item{mocap_assay}{Assay name to use when \code{mocap} is a PhysioExperiment.}

\item{process}{Logical; if \code{TRUE}, runs \code{processEMG()} before combining.}

\item{...}{Additional arguments passed to \code{processEMG()}.}
}
\value{
A list with \code{mocap}, \code{emg_aligned}, and \code{combined} data.frame.
}
\description{
Aligns EMG to MoCap sample times and returns a combined table for downstream
feature analysis.
}
\examples{
mocap <- matrix(rnorm(500), ncol = 5)
emg <- matrix(rnorm(5000), ncol = 2)
out <- integrateEMGMoCap(mocap, emg, mocap_sampling_rate = 100,
                         emg_sampling_rate = 1000)
}
\references{
Merletti R, Parker PA (2004). "Electromyography: Physiology, Engineering,
and Non-Invasive Applications." IEEE Press/Wiley.
}
\seealso{
\code{\link[=processEMG]{processEMG()}} for EMG processing pipeline,
\code{\link[=alignEMGtoMoCap]{alignEMGtoMoCap()}} for time-alignment of EMG to MoCap,
\code{\link[=synchronizeSignals]{synchronizeSignals()}} for general multi-signal synchronization.
}
