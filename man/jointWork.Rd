% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-inverse-dynamics.R
\name{jointWork}
\alias{jointWork}
\title{Integrate joint power into concentric and eccentric work}
\usage{
jointWork(power, sampling_rate, body_mass = NULL, windows = NULL)
}
\arguments{
\item{power}{Numeric vector of joint power (W), or a data frame with a
\code{power} column as returned by \code{computeJointPower(..., split = TRUE)}.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{body_mass}{Optional body mass in kilograms; if supplied, mass-normalised
work columns (\verb{*_per_kg}, J/kg) are added.}

\item{windows}{Optional named list of integer index vectors, each selecting
the samples of one gait cycle or phase (e.g. from \code{\link[=segmentPhases]{segmentPhases()}}). Work
is computed independently within each window. If \code{NULL}, work is computed
over the whole series (window label \code{"full"}).}
}
\value{
A data frame (class \code{joint_work}) with one row per window and columns
\code{window}, \code{concentric_work}, \code{eccentric_work}, and \code{net_work} (J), plus the
mass-normalised counterparts when \code{body_mass} is given.
}
\description{
Integrates a joint power time series (from \code{\link[=computeJointPower]{computeJointPower()}}) with the
trapezoidal rule to obtain concentric work (integral of positive
"generation" power) and eccentric work (integral of negative "absorption"
power). Because the trapezoidal rule is linear in the sampled values,
\code{concentric_work + eccentric_work} equals the net integrated work to
floating-point precision. Work can optionally be split per gait cycle or
movement phase and normalised to body mass.
}
\examples{
t <- seq(0, 1, length.out = 101)
power <- 100 * sin(2 * pi * t)
jointWork(power, sampling_rate = 100, body_mass = 70)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=computeJointPower]{computeJointPower()}}, \code{\link[=labelPowerBursts]{labelPowerBursts()}}.
}
