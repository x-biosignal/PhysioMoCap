% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spasticity-tardieu.R
\name{tardieuScore}
\alias{tardieuScore}
\title{Tardieu score: R1, R2 and the dynamic component}
\usage{
tardieuScore(fast, slow, sampling_rate = NULL, ...)
}
\arguments{
\item{fast}{A \code{\link[=tardieuStretch]{tardieuStretch()}} result (fast stretch), or a raw fast-stretch
angle trace (then scored with \code{onset = "velocity"}).}

\item{slow}{A \code{\link[=tardieuStretch]{tardieuStretch()}} result or a raw slow-stretch angle trace; R2
is its end-range angle (\code{rom_max} / \code{max(angle)}).}

\item{sampling_rate}{Required only when \code{fast} is a raw angle trace.}

\item{...}{Passed to \code{\link[=tardieuStretch]{tardieuStretch()}} when \code{fast} is raw.}
}
\value{
An S3 \code{tardieu_score} list: \code{R1}, \code{R2}, \code{dynamic_component}.
}
\description{
Combines a fast-velocity stretch (the catch angle R1) and a slow-velocity
stretch (the full passive range R2) into the Tardieu dynamic component
\code{R2 - R1} - the velocity-dependent (spastic) part of the range, distinct from
a fixed contracture.
}
\examples{
fs <- 200; t <- seq(0, 0.6, by = 1 / fs)
fast <- ifelse(t <= 0.25, 25 * t / 0.25, 25 + 5 * (t - 0.25) / 0.35)
slow <- 40 * t / 0.6
tardieuScore(tardieuStretch(fast, sampling_rate = fs, onset = "velocity"), slow)
}
\seealso{
\code{\link[=tardieuStretch]{tardieuStretch()}}, \code{\link[=reflexThreshold]{reflexThreshold()}}
}
