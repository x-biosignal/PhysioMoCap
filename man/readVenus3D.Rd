% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-venus3d.R
\name{readVenus3D}
\alias{readVenus3D}
\title{Read Venus3D CSV Data}
\usage{
readVenus3D(path, marker_names = NULL)
}
\arguments{
\item{path}{Path to the Venus3D CSV file.}

\item{marker_names}{Optional character vector of marker names to assign. Must
match the number of markers in the file. If \code{NULL} (default), markers are
labelled \code{P1}, \code{P2}, ... based on the Point Type header, or \code{M1}, \code{M2},
... if that header is absent.}
}
\value{
A PhysioExperiment with assays:
\describe{
\item{position_x}{X coordinates matrix (frames x markers)}
\item{position_y}{Y coordinates matrix (frames x markers)}
\item{position_z}{Z coordinates matrix (frames x markers)}
}

The \code{colData} contains columns \code{label} (marker names) and \code{type}
(\code{"marker"}). The \code{metadata} list contains \code{format}, \code{source_file},
\code{format_version}, \code{units}, \code{coordinate_system}, and \code{time} (numeric vector
of frame times).
}
\description{
Reads motion capture data exported from the OptiTrack -> Motive -> Venus3D
pipeline. The Venus3D CSV format uses \verb{#}-prefixed header lines for metadata
and stores 3D marker coordinates in wide format with columns like \code{1(X)},
\code{1(Y)}, \code{1(Z)}, \code{2(X)}, etc.
}
\details{
Because Venus3D randomly reassigns marker labels across frames, downstream
use of \code{\link[=trackMarkers]{trackMarkers()}} is typically required to establish consistent marker
identities.
}
\examples{
\dontrun{
pe <- readVenus3D("capture.csv")

# Assign meaningful marker names
pe <- readVenus3D("capture.csv",
                  marker_names = c("Hip", "Knee", "Ankle"))

# Follow with marker tracking to resolve label shuffling
pe_tracked <- trackMarkers(pe)
}

}
\references{
Venus3D Software Documentation, C-Motion Inc.
}
\seealso{
\code{\link[=trackMarkers]{trackMarkers()}} for resolving Venus3D's random label assignment,
\code{\link[=readMoCapCSV]{readMoCapCSV()}} for generic CSV formats, \code{\link[=readMoCapAuto]{readMoCapAuto()}} for automatic
format detection.
}
