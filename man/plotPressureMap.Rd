% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis-pressure.R
\name{plotPressureMap}
\alias{plotPressureMap}
\title{Plot a plantar-pressure map}
\usage{
plotPressureMap(
  pm,
  type = c("peak", "mean", "pti", "frame"),
  frame = NULL,
  n_facets = 6L,
  contact_threshold = 0,
  palette = "viridis",
  flip_ap = FALSE
)
}
\arguments{
\item{pm}{A \code{pressure_movie}.}

\item{type}{Map aggregation: \code{"peak"}, \code{"mean"}, \code{"pti"}, or \code{"frame"}.}

\item{frame}{Frame index for \code{type = "frame"}.}

\item{n_facets}{Number of stance windows when explicitly supplied for a
faceted frame plot.}

\item{contact_threshold}{Values at or below this pressure are not drawn.}

\item{palette}{Viridis palette name/option, a single high-end colour, or a
vector of gradient colours.}

\item{flip_ap}{Logical; reverse the displayed anteroposterior axis.}
}
\value{
A \code{ggplot} object.
}
\description{
Renders peak, mean, pressure-time-integral, or individual-frame pressure.
For \code{type = "frame"}, omitting \code{frame} selects the peak-total-pressure frame.
Supplying \code{n_facets} explicitly while omitting \code{frame} instead displays
peak-pressure maps over equal stance windows.
}
\examples{
x <- array(runif(4 * 3 * 10), c(4, 3, 10))
plotPressureMap(pressureMovie(x, 100, dx = 5, dy = 5))
}
