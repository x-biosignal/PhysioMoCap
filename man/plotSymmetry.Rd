% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis-biomech.R
\name{plotSymmetry}
\alias{plotSymmetry}
\title{Plot symmetry comparison (left vs right)}
\usage{
plotSymmetry(
  left,
  right,
  time_axis = NULL,
  ci = 0.95,
  show_diagonal = TRUE,
  plot_type = c("overlay", "scatter"),
  title = "Symmetry Analysis"
)
}
\arguments{
\item{left}{Matrix of left side waveforms (time x observations).}

\item{right}{Matrix of right side waveforms (time x observations).}

\item{time_axis}{Optional time axis values.}

\item{ci}{Confidence interval level.}

\item{show_diagonal}{Logical; show y=x diagonal reference line.}

\item{plot_type}{"overlay" for time series, "scatter" for L vs R scatter.}

\item{title}{Plot title.}
}
\value{
A ggplot object.
}
\description{
Creates a symmetry plot comparing bilateral waveforms, useful for
gait symmetry analysis.
}
\examples{
# Compare left and right knee angles
set.seed(123)
t <- seq(0, 100, length.out = 101)
left <- sapply(1:20, function(i) sin(2*pi*t/100) * 30 + rnorm(101, 0, 2))
right <- sapply(1:20, function(i) sin(2*pi*t/100) * 28 + rnorm(101, 0, 2))  # Slight asymmetry

plotSymmetry(left, right, time_axis = t, plot_type = "overlay")
}
\references{
Wickham H (2016). "ggplot2: Elegant Graphics for Data Analysis." Springer.
}
\seealso{
\code{\link[=plotWaveformComparison]{plotWaveformComparison()}} for multi-group waveform comparisons,
\code{\link[=calculateStepSymmetry]{calculateStepSymmetry()}} for quantifying gait symmetry,
\code{\link[=symmetryIndex]{symmetryIndex()}} for computing symmetry indices.
}
