% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-nonlinear.R
\name{localDynamicStability}
\alias{localDynamicStability}
\title{Local dynamic stability (short- and long-term divergence)}
\usage{
localDynamicStability(
  x,
  stride_samples,
  delay = NULL,
  dim = NULL,
  sampling_rate = 1
)
}
\arguments{
\item{x}{Numeric time series (e.g. trunk acceleration or a joint angle).}

\item{stride_samples}{Samples per stride (sets the divergence windows).}

\item{delay, dim}{Embedding parameters; \code{NULL} estimates them.}

\item{sampling_rate}{Sampling rate in Hz.}
}
\value{
A \code{local_dynamic_stability} object with \code{lambda_short},
\code{lambda_long} (per stride), the \code{divergence} curve, and embedding info.
}
\description{
Local dynamic stability quantifies how a system responds to small natural
perturbations, as the exponential divergence rate of neighbouring trajectories
over the short term (0-1 stride, \code{lambda_short}) and long term
(4-10 strides, \code{lambda_long}), following Dingwell & Cusumano (2000). Higher
divergence means less stable locomotion.
}
\references{
Dingwell JB, Cusumano JJ (2000). Chaos 10(4):848-863.
}
\seealso{
\code{\link[=maxLyapunovExponent]{maxLyapunovExponent()}}
}
