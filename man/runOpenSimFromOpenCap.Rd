% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opensim-integration.R
\name{runOpenSimFromOpenCap}
\alias{runOpenSimFromOpenCap}
\title{Run the local OpenSim toolchain directly from an OpenCap session}
\usage{
runOpenSimFromOpenCap(
  session_id,
  trial_id = NULL,
  api_key = NULL,
  base_url = "https://app.opencap.ai/api",
  model_file = NULL,
  tools = "ik",
  external_loads_file = NULL,
  time_range = NULL,
  workdir = NULL,
  templates = NULL,
  cli = NULL,
  require_external_loads = TRUE,
  dry_run = FALSE
)
}
\arguments{
\item{session_id}{Character string giving the OpenCap session identifier
(the 36-character UUID at the end of the session URL).}

\item{trial_id}{Character string giving the trial identifier within the
session. If \code{NULL} (default), the first trial in the session is used.}

\item{api_key}{Character string with the OpenCap API key. If \code{NULL}
(default), the key is read from the \code{OPENCAP_API_KEY} environment
variable.}

\item{base_url}{Character string giving the base URL for the OpenCap API.
Default is \code{"https://app.opencap.ai/api"}.}

\item{model_file}{Optional local \code{.osim} model; if \code{NULL} (default) it is
downloaded from the session with \code{\link[=downloadOpenCapModel]{downloadOpenCapModel()}}.}

\item{tools, external_loads_file, time_range, workdir, templates, cli, require_external_loads, dry_run}{Passed to \code{\link[=runOpenSimFromMarkers]{runOpenSimFromMarkers()}}.}
}
\value{
An \code{opensim_run} object (see \code{\link[=runOpenSimFromMarkers]{runOpenSimFromMarkers()}}).
}
\description{
End-to-end bridge: downloads an OpenCap session's scaled OpenSim model
(\code{\link[=downloadOpenCapModel]{downloadOpenCapModel()}}) and marker \code{.trc}, then runs the local OpenSim
toolchain (\code{\link[=runOpenSimFromMarkers]{runOpenSimFromMarkers()}}). Requires a working OpenSim backend
(native \pkg{PhysioOpenSim} build or the \code{opensim-cmd} CLI) and, for ID/SO,
an \code{external_loads_file} (OpenCap is markerless and has no ground reactions).
}
\details{
With \code{dry_run = TRUE} the model and markers are still downloaded (they are
needed to write the setups); only the OpenSim tool execution is skipped. For a
fully offline dry run, call \code{\link[=runOpenSimFromMarkers]{runOpenSimFromMarkers()}} with local files.
}
\examples{
\dontrun{
res <- runOpenSimFromOpenCap(
  session_id = "abcd1234-5678-90ab-cdef-1234567890ab",
  tools = c("ik", "id", "so"), external_loads_file = "walk_grf.xml")
res$outputs
}
}
\seealso{
\code{\link[=runOpenSimFromMarkers]{runOpenSimFromMarkers()}}, \code{\link[=readOpenCap]{readOpenCap()}}, \code{\link[=downloadOpenCapModel]{downloadOpenCapModel()}}
}
