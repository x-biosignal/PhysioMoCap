% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gait-pathology.R
\name{classifyGaitPatterns}
\alias{classifyGaitPatterns}
\title{Classify gait pathology patterns from a bundle of gait variables}
\usage{
classifyGaitPatterns(variables, params = list())
}
\arguments{
\item{variables}{A named list of gait-cycle waveforms. Recognised names:
\code{knee_flexion}, \code{ankle_power}, \code{foot_ml}, \code{pelvic_obliquity}.}

\item{params}{Optional named list of per-pattern argument lists forwarded to
the individual detectors (e.g. \code{list(stiff_knee = list(threshold = 40))}).}
}
\value{
A data frame (class \code{gait_pattern_classification}) with columns
\code{pattern}, \code{flagged}, \code{severity}, \code{metric}, \code{threshold}, one row per
evaluated pattern.
}
\description{
Runs each applicable detector on the supplied gait-cycle waveforms and
returns a per-pattern flag and severity. A pattern is evaluated only when its
input variable is present in \code{variables}.
}
\examples{
vars <- list(knee_flexion = 60 * sin(seq(0, pi, length.out = 101)))
classifyGaitPatterns(vars)
}
\seealso{
\code{\link[=gaitPatternLibrary]{gaitPatternLibrary()}}
}
