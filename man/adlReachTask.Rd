% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adl-reach.R
\name{adlReachTask}
\alias{adlReachTask}
\title{Assess an upper-limb ADL reach from its speed profile}
\usage{
adlReachTask(
  speed,
  sampling_rate,
  task = c("reaching", "drinking", "feeding", "dressing", "grooming"),
  onset_threshold = 0.05
)
}
\arguments{
\item{speed}{Numeric hand/end-effector speed profile (one reach).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{task}{ADL task: \code{"reaching"} (default, generic hand-arm use, d445),
\code{"drinking"} (d560), \code{"feeding"} (eating, d550), \code{"dressing"} (d540) or
\code{"grooming"} (d520).}

\item{onset_threshold}{Movement-onset threshold (fraction of peak speed).}
}
\value{
an \code{adl_reach_task} list: \code{task}, \code{icf_code}, and the kinematics
\code{movement_time}, \code{peak_velocity}, \code{time_to_peak_frac}, \code{movement_units}
(submovements; 1 = a single smooth transport), \code{sparc} and \code{ldlj}
(smoothness; less negative = smoother).
}
\description{
Computes the standard reach-to-grasp kinematics for one ADL arm transport and
labels it with the ADL task and its ICF code. Works directly on the
end-effector (hand) speed profile, so it needs no full marker set; derive the
speed from a trajectory with \code{\link[=computeSpeed]{computeSpeed()}} or pass a hand-sensor speed.
}
\examples{
# a single smooth (minimum-jerk) reach: one movement unit
fs <- 100; tt <- seq(0, 1, 1 / fs)
v <- 30 * (tt^2) * (1 - tt)^2                 # bell-shaped speed
adlReachTask(v, fs, task = "drinking")$movement_units
}
\references{
Rohrer B, et al. (2002) J Neurosci 22:8297-8304 (movement units);
Balasubramanian S, et al. (2015) IEEE TBME 62:2137-2147 (SPARC).
}
\seealso{
\code{\link[=reachingKinematics]{reachingKinematics()}}, \code{\link[=movementUnits]{movementUnits()}}, \code{\link[=sparc]{sparc()}}
}
