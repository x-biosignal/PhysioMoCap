% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-inverse-dynamics.R
\name{inverseDynamics2D}
\alias{inverseDynamics2D}
\title{Compute 2D lower-limb joint moments with inverse dynamics}
\usage{
inverseDynamics2D(
  joints,
  grf,
  sampling_rate,
  angles = NULL,
  angular_velocity = NULL,
  angular_acceleration = NULL,
  inertial = NULL,
  angle_unit = c("radian", "degree"),
  model = c("newton_euler", "quasi_static"),
  body_mass = NULL,
  body_height = NULL,
  gravity = .GRAVITY
)
}
\arguments{
\item{joints}{Matrix/data.frame with columns \code{ankle_x}, \code{ankle_y},
\code{knee_x}, \code{knee_y}, \code{hip_x}, \code{hip_y}, and - for
\code{model = "newton_euler"} - the foot distal end \code{toe_x}, \code{toe_y}.}

\item{grf}{Matrix/data.frame with at least \code{fx} and \code{fy} columns, and
optionally \code{cop_x} and \code{cop_y}.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{angles}{Optional matrix/data.frame with columns \code{ankle}, \code{knee},
\code{hip} (joint angles).}

\item{angular_velocity}{Optional matrix/data.frame with columns
\code{ankle}, \code{knee}, \code{hip}.}

\item{angular_acceleration}{Optional matrix/data.frame with columns
\code{ankle}, \code{knee}, \code{hip}.}

\item{inertial}{Optional data.frame from \code{estimateSegmentInertia()}. Required
for \code{model = "newton_euler"} unless \code{body_mass} is given; under
\code{model = "quasi_static"} it only supplies an \code{I * alpha} correction and is
ignored when omitted.}

\item{angle_unit}{Unit of \code{angles}: \code{"radian"} or \code{"degree"}.}

\item{model}{Which dynamics model to use. \code{"newton_euler"} (the default)
runs the full recursive link-segment Newton-Euler chain via
\code{\link[=inverseDynamicsRNE]{inverseDynamicsRNE()}}. \code{"quasi_static"} is the legacy massless-segment
approximation, retained only for reproducing older results (see Details).}

\item{body_mass}{Body mass in kg. Used to build the segment inertia table
with \code{\link[=estimateSegmentInertia]{estimateSegmentInertia()}} when \code{inertial} is not supplied.}

\item{body_height}{Body height in m, passed to \code{\link[=estimateSegmentInertia]{estimateSegmentInertia()}}
when segment lengths must be estimated from stature. If \code{NULL}, segment
lengths are measured from the marker data with \code{\link[=scaleBodyModel]{scaleBodyModel()}}.}

\item{gravity}{Gravitational acceleration in m/s^2.}
}
\value{
A data.frame with time index and joint moments (\code{ankle_moment},
\code{knee_moment}, \code{hip_moment}). If angular velocity is available,
corresponding power columns are included. \code{model = "newton_euler"} also
returns the proximal joint reaction forces (\code{ankle_fx}, \code{ankle_fy}, ...).
}
\description{
Computes sagittal-plane ankle, knee, and hip net moments from joint-center
coordinates, GRF, COP, and segment inertial properties, using a recursive
link-segment Newton-Euler chain (foot -> shank -> thigh).
}
\details{
The default \code{"newton_euler"} model propagates reactions distal to proximal:
the ground reaction force and free moment enter at the foot, and every
segment contributes its weight \eqn{m g}, its linear inertia
\eqn{m a_{com}} and its angular inertia \eqn{I \alpha}. Segment centres of
mass come from the anthropometric ratios in \code{inertial}, and their linear and
angular accelerations from numerical differentiation of the marker
trajectories, so a segment mass source (\code{inertial} or \code{body_mass}) and the
foot distal end (\code{toe_x}, \code{toe_y}) are required.

Because the recursion differentiates the marker trajectories twice, it
amplifies marker noise: the joint moments are only as good as the smoothing
applied beforehand. On a static limb sampled at 200 Hz, 1 mm of white marker
noise produces a spurious moment about 24 times the true value; a 6 Hz
zero-lag low-pass (\code{\link[=butterworthFilter]{butterworthFilter()}}, or \code{\link[=filterSignals]{filterSignals()}} on the
positions) reduces that by more than an order of magnitude. Filter the marker
data before calling this function. The quasi-static model never
differentiated positions, so this exposure is new.

\code{"quasi_static"} reproduces the pre-1.0 behaviour: each joint moment is the
moment of the \emph{ground reaction force alone} about that joint centre, plus an
optional \eqn{I \alpha} term. It omits segment weight and linear inertia
entirely, so it returns identically zero whenever the limb is off the ground
(the whole swing phase) and underestimates stance moments - at the hip by
roughly 8\% in quiet standing. It is kept only so that analyses published
against the old implementation can be reproduced, and emits a message when
selected.
}
\examples{
n <- 200
joints <- data.frame(
  ankle_x = rep(0.00, n), ankle_y = rep(0.05, n),
  toe_x   = rep(0.15, n), toe_y   = rep(0.01, n),
  knee_x = rep(0.00, n),  knee_y = rep(0.45, n),
  hip_x = rep(0.00, n),   hip_y = rep(0.85, n)
)
grf <- data.frame(fx = rep(0, n), fy = abs(sin(seq(0, pi, length.out = n))) * 800,
                  cop_x = rep(0.02, n), cop_y = rep(0, n))
id <- inverseDynamics2D(joints, grf, sampling_rate = 100, body_mass = 70)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=inverseDynamicsRNE]{inverseDynamicsRNE()}} for the underlying recursion,
\code{\link[=inverseDynamics3D]{inverseDynamics3D()}} for 3D inverse dynamics computation,
\code{\link[=estimateSegmentInertia]{estimateSegmentInertia()}} for segment inertial properties,
\code{\link[=computeJointPower]{computeJointPower()}} for joint power calculation.
}
