% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomech-com.R
\name{calculateCOM}
\alias{calculateCOM}
\title{Calculate whole-body center of mass from marker positions}
\usage{
calculateCOM(pe, body_mass, skeleton = NULL, bsip = NULL, marker_map = NULL)
}
\arguments{
\item{pe}{A \code{PhysioExperiment} object containing \code{position_x},
\code{position_y}, and optionally \code{position_z} assays.}

\item{body_mass}{Numeric scalar, body mass in kilograms. Must be
positive.}

\item{skeleton}{Optional \code{SkeletonModel} object used for automatic
marker mapping. If provided and \code{marker_map} is \code{NULL}, a default
mapping is generated from the skeleton model.}

\item{bsip}{Optional data.frame of body segment parameters (as
returned by \code{\link[=segmentParameters]{segmentParameters()}}). If \code{NULL} (default), uses
\code{segmentParameters("deLeva_male")}.}

\item{marker_map}{Optional named list mapping each segment to its
proximal and distal marker names. Each element should be a character
vector of length 2: \code{c(proximal_marker, distal_marker)}. Segment
names must match those in the \code{bsip} table. Example:
\code{list(thigh_r = c("RHip", "RKnee"), thigh_l = c("LHip", "LKnee"))}.}
}
\value{
A \code{PhysioExperiment} object with additional assays \code{com_x},
\code{com_y}, and (if 3D) \code{com_z}. Each assay is a single-column matrix
with column name \code{"COM"} and the same number of rows as the input.
}
\description{
Computes the whole-body center of mass (COM) at each time frame from
3D (or 2D) marker position data and body segment inertial parameters.
}
\details{
The algorithm proceeds as follows:
\enumerate{
\item For each body segment, compute the segment COM position as:
\deqn{COM_{seg} = P_{prox} + f \times (P_{dist} - P_{prox})}
where \eqn{f} is the COM proximal fraction from the BSIP table.
\item Compute the whole-body COM as the mass-weighted average of
all segment COMs:
\deqn{COM_{body} = \frac{\sum_i m_i \times COM_i}{M}}
where \eqn{m_i = M \times f_i} is the segment mass and \eqn{M}
is the total body mass.
}
}
\examples{
\dontrun{
pe <- readTRC("markers.trc")
result <- calculateCOM(pe, body_mass = 75)
com_x <- SummarizedExperiment::assay(result, "com_x")
}
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.

Zatsiorsky VM (2002). "Kinetics of Human Motion." Human Kinetics.
}
\seealso{
\code{\link[=segmentParameters]{segmentParameters()}} for body segment inertial parameters,
\code{\link[=calculateSegmentCOM]{calculateSegmentCOM()}} for individual segment center of mass,
\code{\link[=symmetryIndex]{symmetryIndex()}} for bilateral symmetry assessment.
}
