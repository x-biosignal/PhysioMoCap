% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-deeplabcut.R
\name{readDeepLabCut}
\alias{readDeepLabCut}
\title{Read DeepLabCut output}
\usage{
readDeepLabCut(path, fps = 30, format = c("csv", "h5"))
}
\arguments{
\item{path}{Path to a DeepLabCut output file (CSV or H5).}

\item{fps}{Frame rate in Hz (frames per second). Default 30.
DeepLabCut does not store frame rate in its output, so this must be
specified by the user.}

\item{format}{File format: \code{"csv"} or \code{"h5"}. Default \code{"csv"}.}
}
\value{
A PhysioExperiment with assays:
\describe{
\item{keypoint_x}{X coordinates matrix (frames x bodyparts)}
\item{keypoint_y}{Y coordinates matrix (frames x bodyparts)}
\item{confidence}{Detection likelihood matrix (frames x bodyparts)}
}

The \code{colData} contains columns \code{label} (bodypart names), \code{type}
(\code{"keypoint"}), and \code{scorer} (the DLC scorer/model name).

The \code{metadata} list contains \code{dlc_scorer}, \code{format}, and \code{source_file}.
}
\description{
Reads DeepLabCut (DLC) pose estimation output from CSV or HDF5 format
and returns a PhysioExperiment object with keypoint coordinates and
confidence scores.
}
\details{
DeepLabCut outputs pose estimation results in CSV or HDF5 format.

\strong{CSV format} has a 3-row multi-level header:
\enumerate{
\item Row 1: scorer name (the DLC model name)
\item Row 2: bodypart names
\item Row 3: coordinate type (x, y, likelihood)
}
Followed by numeric data where each row is a frame and columns are
grouped as \verb{[x, y, likelihood]} triplets per bodypart.

\strong{H5 format} stores data in a hierarchical HDF5 structure under
\code{df_with_missing/table}. Requires the \code{rhdf5} package.
}
\examples{
\dontrun{
# Read DeepLabCut CSV output
pe <- readDeepLabCut("path/to/DLC_output.csv", fps = 30)

# Read HDF5 format
pe <- readDeepLabCut("path/to/DLC_output.h5", fps = 25, format = "h5")
}

}
\references{
Mathis A, Mamidanna P, Cury KM, Abe T, Murthy VN, Mathis MW, Bethge M
(2018). "DeepLabCut: markerless pose estimation of user-defined body
parts with deep learning." Nature Neuroscience, 21(9), 1281-1289.
}
\seealso{
\code{\link[=readOpenPose]{readOpenPose()}}, \code{\link[=readMediaPipe]{readMediaPipe()}}, \code{\link[=readOpenCap]{readOpenCap()}}
}
