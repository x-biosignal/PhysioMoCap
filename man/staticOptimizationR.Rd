% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/static-optimization.R
\name{staticOptimizationR}
\alias{staticOptimizationR}
\title{Pure-R static optimization of muscle activations}
\usage{
staticOptimizationR(
  moment_arms,
  max_force,
  joint_moments,
  activation_bounds = c(0, 1),
  weights = NULL,
  passive_moments = NULL
)
}
\arguments{
\item{moment_arms}{Muscle moment arms: either a single \verb{n_dof x n_muscle}
matrix \code{r} (moment arm of muscle \emph{i} about degree-of-freedom \emph{j}, reused for
every frame) or a list of such matrices, one per frame.}

\item{max_force}{Numeric vector of muscle maximum isometric forces
(\code{F_max}, length \code{n_muscle}).}

\item{joint_moments}{Net joint moments to reproduce: a length-\code{n_dof} vector
(single frame) or an \verb{n_frames x n_dof} matrix (one row per frame).}

\item{activation_bounds}{Length-2 numeric \code{c(lower, upper)} activation bounds
(default \code{c(0, 1)}).}

\item{weights}{Optional length-\code{n_muscle} positive weights \code{w_i} for the
effort criterion (default all 1).}

\item{passive_moments}{Optional passive/gravity joint moments subtracted from
\code{joint_moments} before optimization (same shape as \code{joint_moments}).}
}
\value{
A \code{static_optimization_r} object: a list with \code{activations}
(\verb{n_frames x n_muscle}), \code{moments} (the reproduced net joint moments,
\verb{n_frames x n_dof}), \code{residual} (max absolute moment error per frame),
\code{effort} (sum of squared activations per frame), \code{feasible} (logical per
frame), and the muscle / DOF counts.
}
\description{
Distributes the net joint moment at each time frame across muscles by
minimising the sum of squared activations (a quadratic muscle-effort
criterion), subject to the moments being reproduced exactly and each
activation staying within its bounds:
\deqn{\min_a \sum_i w_i a_i^2 \quad \text{s.t.} \quad
      \tau_j = \sum_i a_i F^{max}_i r_{ji}, \; 0 \le a_i \le 1.}
Each frame is an independent convex quadratic program (solved with
\pkg{quadprog}), giving the unique minimum-effort activations. This mirrors
the OpenSim Static Optimization tool (Anderson & Pandy 2001) and is used by
\code{\link[=runStaticOptimization]{runStaticOptimization()}} when OpenSim is unavailable.
}
\examples{
if (requireNamespace("quadprog", quietly = TRUE)) {
  # Two muscles with unit moment arm and F_max reproduce a moment of 30.
  r <- matrix(c(1, 1), nrow = 1)
  so <- staticOptimizationR(r, max_force = c(100, 100), joint_moments = 30)
  so$activations
}
}
\references{
Anderson FC, Pandy MG (2001). "Static and dynamic optimization solutions for
gait are practically equivalent." J Biomech 34(2):153-161.
}
\seealso{
\code{\link[=runStaticOptimization]{runStaticOptimization()}}
}
