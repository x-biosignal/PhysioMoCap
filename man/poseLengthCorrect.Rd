% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pose-fix.R
\name{poseLengthCorrect}
\alias{poseLengthCorrect}
\title{Standardise segment lengths (camera-distance correction)}
\usage{
poseLengthCorrect(pe, chains = NULL, reference = c("median", "mean"))
}
\arguments{
\item{pe}{A pose \code{PhysioExperiment} (assays \code{keypoint_x}, \code{keypoint_y};
\code{colData$label}), e.g. from \code{\link[=readOpenPose]{readOpenPose()}} or after \code{\link[=poseFix]{poseFix()}}.}

\item{chains}{Optional list of kinematic chains, each a proximal-to-distal
vector of keypoint names (default: both legs and both arms, restricted to
the present keypoints).}

\item{reference}{\code{"median"} (default, robust) or \code{"mean"} reference length.}
}
\value{
A \code{PhysioExperiment} with length-standardised \code{keypoint_x}/\code{y};
\code{metadata()$poseLengthCorrect} holds the reference length per segment.
}
\description{
In 2D video, apparent bone lengths shrink and grow as the subject moves toward
or away from the camera, biasing kinematics. This rescales each limb segment so
its length matches a stable reference (the recording's median or mean of that
segment), repositioning the distal keypoint along the bone direction and
chaining proximal to distal. Generalised form of the length correction in
PoseFixeR (Sugiyama, Uno & Matsui 2023).
}
\references{
Sugiyama S, Uno K, Matsui Y (2023). \emph{PLOS Comput Biol}
19:e1009989. \doi{10.1371/journal.pcbi.1009989}
}
\seealso{
\code{\link[=poseFix]{poseFix()}}
}
