% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/imu-gait.R
\name{detectStanceZUPT}
\alias{detectStanceZUPT}
\title{Detect foot-IMU stance phases for zero-velocity updates}
\usage{
detectStanceZUPT(
  accel,
  gyro,
  sampling_rate,
  g = 9.81,
  method = c("magnitude", "shoe"),
  accel_threshold = 0.6,
  gyro_threshold = 0.6,
  sigma_a = 0.05,
  sigma_g = 0.05,
  gamma = 10000,
  window = NULL,
  min_stance = NULL
)
}
\arguments{
\item{accel}{Numeric matrix (n x 3) of accelerometer readings (m/s^2).}

\item{gyro}{Numeric matrix (n x 3) of gyroscope readings (rad/s).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{g}{Gravitational acceleration magnitude (default 9.81 m/s^2).}

\item{method}{\code{"magnitude"} (default) or \code{"shoe"}.}

\item{accel_threshold}{Magnitude method: max \verb{|‖accel‖ - g|} for stance
(default 0.6 m/s^2).}

\item{gyro_threshold}{Magnitude method: max \verb{‖gyro‖} for stance
(default 0.6 rad/s).}

\item{sigma_a, sigma_g}{SHOE method: accelerometer / gyroscope noise standard
deviations (default 0.05 and 0.05).}

\item{gamma}{SHOE method: test-statistic threshold below which a sample is
stance (default 1e4).}

\item{window}{Number of samples in the SHOE sliding window (default is about
50 ms of data).}

\item{min_stance}{Minimum stance-run length in samples; shorter runs are
discarded as spurious (default is about 50 ms of data).}
}
\value{
A logical vector of length \code{n}, \code{TRUE} at stance samples.
}
\description{
Flags the stationary (foot-flat) samples of a foot-mounted IMU trace, used as
zero-velocity-update (ZUPT) anchors by \code{\link[=strapdownIntegrate]{strapdownIntegrate()}} and
\code{\link[=footImuGait]{footImuGait()}}. The default \code{"magnitude"} detector marks a sample as stance
when the accelerometer magnitude is close to gravity \emph{and} the gyroscope
magnitude is small; the \code{"shoe"} detector is the SHOE (Stance Hypothesis
Optimal Estimation) generalised-likelihood-ratio test of Skog et al. (2010).
}
\references{
Skog I, Handel P, Nilsson J-O, Rantakokko J (2010). "Zero-velocity detection
-- an algorithm evaluation." IEEE Trans Biomed Eng 57(11):2657-2666.
}
\seealso{
\code{\link[=strapdownIntegrate]{strapdownIntegrate()}}, \code{\link[=footImuGait]{footImuGait()}}.
}
