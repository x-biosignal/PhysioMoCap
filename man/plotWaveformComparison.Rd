% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis-biomech.R
\name{plotWaveformComparison}
\alias{plotWaveformComparison}
\title{Plot waveform comparison across groups}
\usage{
plotWaveformComparison(
  x,
  groups,
  channel = 1L,
  ci = 0.95,
  show_individual = FALSE,
  time_axis = NULL,
  colors = NULL,
  title = NULL,
  xlab = "Time",
  ylab = "Value"
)
}
\arguments{
\item{x}{A PhysioExperiment object or matrix (time x observations).}

\item{groups}{Factor or vector indicating group membership.}

\item{channel}{For PhysioExperiment with multiple channels, which to plot.}

\item{ci}{Confidence interval level (default: 0.95).}

\item{show_individual}{Logical; show individual waveforms as thin lines.}

\item{time_axis}{Optional time axis values (e.g., 0-100 for gait cycle).}

\item{colors}{Optional color palette for groups.}

\item{title}{Plot title.}

\item{xlab}{X-axis label.}

\item{ylab}{Y-axis label.}
}
\value{
A ggplot object.
}
\description{
Creates a comparison plot showing mean waveforms with confidence bands
for multiple groups. Ideal for comparing gait patterns between conditions.
}
\examples{
# Compare gait patterns between groups
set.seed(123)
# Control group
control <- sapply(1:15, function(i) sin(seq(0, 2*pi, length.out = 101)) * 30 + rnorm(101, 0, 3))
# Patient group (reduced range of motion)
patient <- sapply(1:15, function(i) sin(seq(0, 2*pi, length.out = 101)) * 20 + rnorm(101, 0, 3))

data <- cbind(control, patient)
groups <- factor(rep(c("Control", "Patient"), each = 15))

plotWaveformComparison(data, groups, time_axis = 0:100,
                       xlab = "Gait Cycle (\%)", ylab = "Knee Angle (deg)")
}
\references{
Wickham H (2016). "ggplot2: Elegant Graphics for Data Analysis." Springer.
}
\seealso{
\code{\link[=plotGaitCycle]{plotGaitCycle()}} for single-group gait cycle visualization,
\code{\link[=plotSymmetry]{plotSymmetry()}} for left-right symmetry plots,
\code{\link[=plotSpaghetti]{plotSpaghetti()}} for individual waveform overlay plots.
}
