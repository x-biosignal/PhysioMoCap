% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-inverse-dynamics.R
\name{computeJointPower}
\alias{computeJointPower}
\title{Compute joint power from moment and angular velocity}
\usage{
computeJointPower(moment, angular_velocity, split = FALSE)
}
\arguments{
\item{moment}{Numeric vector of joint moment values.}

\item{angular_velocity}{Numeric vector of joint angular velocity in rad/s.}

\item{split}{Logical; if \code{TRUE}, return a data frame with the total \code{power}
split into non-negative \code{generation} (\code{P > 0}) and non-positive
\code{absorption} (\code{P < 0}) components. Default \code{FALSE} returns the total power
vector (backward compatible).}
}
\value{
If \code{split = FALSE}, a numeric vector of joint power (W). If
\code{split = TRUE}, a data frame with columns \code{power}, \code{generation}, and
\code{absorption} (each in W); \code{generation + absorption == power} elementwise.
}
\description{
Joint power is the product of the net joint moment and the joint angular
velocity. Positive power (\code{generation}) reflects concentric muscle action
doing work on the segment; negative power (\code{absorption}) reflects eccentric
action absorbing energy. Set \code{split = TRUE} to return the generation and
absorption components alongside the total.
}
\examples{
computeJointPower(c(10, 12, 8), c(2, 1.5, 1))
computeJointPower(c(10, -12, 8), c(2, 1.5, -1), split = TRUE)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=inverseDynamics2D]{inverseDynamics2D()}} for computing joint moments in 2D,
\code{\link[=inverseDynamics3D]{inverseDynamics3D()}} for computing joint moments in 3D,
\code{\link[=jointWork]{jointWork()}} for integrating power into concentric/eccentric work,
\code{\link[=labelPowerBursts]{labelPowerBursts()}} for Winter power-burst nomenclature.
}
