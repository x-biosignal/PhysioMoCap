% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-nonlinear.R
\name{sampleEntropy}
\alias{sampleEntropy}
\title{Sample entropy (Richman & Moorman)}
\usage{
sampleEntropy(x, m = 2L, r = 0.2, normalize = TRUE)
}
\arguments{
\item{x}{Numeric time series.}

\item{m}{Embedding (template) length (default 2).}

\item{r}{Similarity tolerance. If \code{normalize = TRUE} (default) it is a
multiple of the series standard deviation (default \code{0.2}).}

\item{normalize}{If \code{TRUE}, \code{r} is scaled by \code{sd(x)}.}
}
\value{
A single numeric sample-entropy value (\code{Inf} if no length-\code{m+1}
matches occur).
}
\description{
Sample entropy quantifies the irregularity of a time series as the negative
natural log of the conditional probability that sequences similar for \code{m}
points remain similar at the next point, within tolerance \code{r} (self-matches
excluded). Larger values indicate greater irregularity/complexity; a perfectly
regular signal tends to zero.
}
\examples{
sampleEntropy(sin(seq(0, 40 * pi, length.out = 800)))  # ~0 (regular)
}
\references{
Richman JS, Moorman JR (2000). Am J Physiol 278(6):H2039-H2049.
}
\seealso{
\code{\link[=maxLyapunovExponent]{maxLyapunovExponent()}}
}
