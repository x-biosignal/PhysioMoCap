% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/movement-tremor.R
\name{tremorMetrics}
\alias{tremorMetrics}
\title{Tremor metrics (frequency + amplitude, with a task tag)}
\usage{
tremorMetrics(
  signal,
  sampling_rate,
  condition = c("rest", "postural", "kinetic"),
  band = c(3, 12),
  ...
)
}
\arguments{
\item{signal}{Numeric movement signal (one axis or magnitude).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{condition}{Recording condition: \code{"rest"}, \code{"postural"} or \code{"kinetic"}.}

\item{band}{Length-2 tremor band \code{c(low, high)} in Hz (default \code{c(3, 12)}).}

\item{...}{Passed to \code{\link[=tremorSpectrum]{tremorSpectrum()}}.}
}
\value{
An S3 \code{tremor_metrics} list: \code{dominant_freq_hz}, \code{band_power_abs},
\code{band_power_rel}, \code{rms_amplitude}, \code{half_power_bw_hz}, \code{harmonic_ratio},
\code{condition}, \code{band}, \code{sampling_rate}.
}
\description{
Combines \code{\link[=tremorSpectrum]{tremorSpectrum()}} and \code{\link[=tremorAmplitude]{tremorAmplitude()}} into a single tremor
summary, tagged with the recording condition. The rest / postural / kinetic
distinction is task-context metadata supplied by the caller (the segment
recorded under that condition), not a separate algorithm.
}
\examples{
fs <- 100; t <- seq(0, 20, by = 1 / fs)
tremorMetrics(1.5 * sin(2 * pi * 6 * t), fs, condition = "postural")
}
\seealso{
\code{\link[=tremorSpectrum]{tremorSpectrum()}}, \code{\link[=tremorAmplitude]{tremorAmplitude()}}
}
