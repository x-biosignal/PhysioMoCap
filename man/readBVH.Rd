% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-bvh.R
\name{readBVH}
\alias{readBVH}
\title{Read BVH Skeleton Animation File (.bvh)}
\usage{
readBVH(path)
}
\arguments{
\item{path}{Character string giving the path to the \code{.bvh} file.}
}
\value{
A \code{PhysioExperiment} object with rotation assays (\code{rotation_x},
\code{rotation_y}, \code{rotation_z}) for all joints, and position assays
(\code{position_x}, \code{position_y}, \code{position_z}) for the root joint.
\code{colData} includes joint names (\code{label}), channel type (\code{type}), and
\code{parent_joint}. \code{metadata} includes \code{bvh_skeleton} (hierarchy tree),
\code{rotation_order}, \code{frame_time}, \code{offsets}, and \code{source_file}.
\code{samplingRate} is computed as \code{1 / frame_time}.
}
\description{
Reads a BVH (Biovision Hierarchy) file containing skeleton animation data.
BVH files have two sections: HIERARCHY (joint tree structure with offsets
and channel definitions) and MOTION (frame data). The ROOT joint has 6
channels (3 position + 3 rotation), while child JOINTs have 3 channels
(rotation only).
}
\examples{
bvh_file <- system.file("testdata", "sample.bvh", package = "PhysioMoCap")
if (nzchar(bvh_file)) {
  pe <- readBVH(bvh_file)
  pe
}
}
\references{
Meredith M, Maddock S (2001). "Motion Capture File Formats Explained."
Department of Computer Science, University of Sheffield.
}
\seealso{
\code{\link[=readC3D]{readC3D()}}, \code{\link[=readTRC]{readTRC()}}, \code{\link[=readOpenPose]{readOpenPose()}}
}
