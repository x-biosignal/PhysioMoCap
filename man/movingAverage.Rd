% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-filter.R
\name{movingAverage}
\alias{movingAverage}
\title{Simple moving average filter}
\usage{
movingAverage(x, window = 5)
}
\arguments{
\item{x}{A numeric vector or matrix (time x channels).}

\item{window}{Window size for the moving average (default: 5). Will be
coerced to an odd integer.}
}
\value{
Smoothed data with the same dimensions as \code{x}. Edge values where
the full window cannot be applied are set to NA.
}
\description{
Applies a symmetric moving average for quick smoothing of signal data.
Uses \code{\link[stats:filter]{stats::filter()}} internally with equal weights.
}
\examples{
# Smooth a noisy signal
x <- sin(seq(0, 4 * pi, length.out = 200)) + rnorm(200, sd = 0.3)
x_smooth <- movingAverage(x, window = 7)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=butterworthFilter]{butterworthFilter()}} for Butterworth low-pass filtering,
\code{\link[=savgolFilter]{savgolFilter()}} for Savitzky-Golay polynomial smoothing.
}
