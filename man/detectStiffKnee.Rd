% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gait-pathology.R
\name{detectStiffKnee}
\alias{detectStiffKnee}
\title{Detect stiff-knee gait (reduced / delayed peak swing knee flexion)}
\usage{
detectStiffKnee(
  knee_flexion,
  swing = c(60, 100),
  normal_peak = 60,
  threshold = 45,
  severe_floor = 10,
  delay_threshold = 80
)
}
\arguments{
\item{knee_flexion}{Knee flexion angle (degrees, flexion positive) over one
normalised gait cycle.}

\item{swing}{Percentage window of swing phase (default \code{c(60, 100)}).}

\item{normal_peak}{Normal peak swing knee flexion in degrees (default 60).}

\item{threshold}{Peak below which stiff-knee is flagged (default 45).}

\item{severe_floor}{Peak flexion mapped to severity 1 (default 10).}

\item{delay_threshold}{Peak-timing percentage above which the peak is
considered delayed (default 80).}
}
\value{
A \code{gait_pattern_flag} object.
}
\description{
Stiff-knee gait is marked by reduced peak knee flexion in swing (normal
\eqn{\approx 60^{\circ}} near 73\\% of the cycle), often with delayed timing
(Perry & Burnfield; Goldberg et al. 2006).
}
\examples{
# normal knee flexion peaks ~60 deg in swing
k <- c(seq(5, 18, length.out = 30), seq(18, 5, length.out = 30),
       60 * sin(seq(0, pi, length.out = 41)))
detectStiffKnee(k)
}
\references{
Perry J, Burnfield JM (2010); Goldberg SR, et al. (2006).
}
\seealso{
\code{\link[=classifyGaitPatterns]{classifyGaitPatterns()}}, \code{\link[=gaitPatternLibrary]{gaitPatternLibrary()}}
}
