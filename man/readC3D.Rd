% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-c3d.R
\name{readC3D}
\alias{readC3D}
\title{Read C3D Motion Capture File}
\usage{
readC3D(path, include_analog = FALSE)
}
\arguments{
\item{path}{Character string giving the path to the \code{.c3d} file.}

\item{include_analog}{Logical; if \code{TRUE}, analog data (e.g., force plate
channels) is extracted and stored in \code{metadata(pe)$analog_data} as a
data frame. Default is \code{FALSE}.}
}
\value{
A \code{PhysioExperiment} object with three assays: \code{"position_x"},
\code{"position_y"}, and \code{"position_z"}, each a matrix with rows as time
frames and columns as markers. If the C3D file contains residual data,
a \code{"quality"} assay is also included. Column metadata (\code{colData})
contains \code{label} (marker names from POINT:LABELS), \code{type} (\code{"marker"}),
and \code{body_segment} (\code{NA}). Metadata includes \code{c3d_parameters},
\code{source_file}, and a \code{time} vector computed from frame rate.
}
\description{
Reads a C3D file containing 3D marker position data using the \pkg{c3dr}
package. C3D is a widely used binary format for storing biomechanical
motion capture data including point (marker) positions and optional
analog channel data (e.g., force plate signals).
}
\examples{
if (requireNamespace("c3dr", quietly = TRUE)) {
  c3d_file <- c3dr::c3d_example()
  pe <- readC3D(c3d_file)
  pe
}
}
\references{
C3D.org. "The C3D File Format." \url{https://www.c3d.org/}.
}
\seealso{
\code{\link[=readTRC]{readTRC()}}, \code{\link[=readBVH]{readBVH()}}, \code{\link[=readOpenPose]{readOpenPose()}}
}
