% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-nonlinear.R
\name{maxLyapunovExponent}
\alias{maxLyapunovExponent}
\title{Largest Lyapunov exponent (Rosenstein)}
\usage{
maxLyapunovExponent(
  x,
  delay = NULL,
  dim = NULL,
  sampling_rate = 1,
  mean_period = NULL,
  max_steps = NULL,
  fit_range = NULL
)
}
\arguments{
\item{x}{Numeric time series.}

\item{delay, dim}{Embedding delay and dimension; \code{NULL} estimates them via
\code{\link[=timeDelayEmbed]{timeDelayEmbed()}}.}

\item{sampling_rate}{Sampling rate in Hz (scales the exponent to per-second).}

\item{mean_period}{Theiler window in samples excluding temporally-close
neighbours; \code{NULL} estimates it from the mean signal period.}

\item{max_steps}{Number of forward steps to track divergence; \code{NULL} uses a
default derived from the series length.}

\item{fit_range}{Integer vector \code{c(from, to)} (in steps) of the divergence
curve to fit the slope; \code{NULL} uses an early near-linear window.}
}
\value{
A \code{lyapunov_exponent} object with \code{lambda} (per second), the
\code{divergence} curve, \code{fit_range}, \code{delay} and \code{dim}.
}
\description{
Estimates the largest Lyapunov exponent from the average logarithmic
divergence of initially nearby trajectories in a delay-embedded phase space
(Rosenstein et al. 1993). A positive exponent indicates sensitive dependence
on initial conditions (chaos / local instability).
}
\references{
Rosenstein MT, Collins JJ, De Luca CJ (1993). Physica D 65:117-134.
}
\seealso{
\code{\link[=localDynamicStability]{localDynamicStability()}}, \code{\link[=timeDelayEmbed]{timeDelayEmbed()}}
}
