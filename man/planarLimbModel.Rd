% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-forward-dynamics.R
\name{planarLimbModel}
\alias{planarLimbModel}
\title{Planar two-segment limb model (equations of motion)}
\usage{
planarLimbModel(
  mass,
  length,
  com = length/2,
  inertia = mass * length^2/12,
  g = 9.81
)
}
\arguments{
\item{mass}{Length-2 segment masses (kg).}

\item{length}{Length-2 segment lengths (m).}

\item{com}{Length-2 distances of each segment centre of mass from its proximal
joint (m); default \code{length / 2}.}

\item{inertia}{Length-2 segment moments of inertia about the CoM (kg m^2);
default the thin-rod \code{mass * length^2 / 12}.}

\item{g}{Gravitational acceleration (m/s^2, default 9.81; use 0 for a
horizontal-plane / no-gravity model).}
}
\value{
a \code{planar_limb_model}: functions \code{M(q)}, \code{coriolis(q, qd)} (matrix),
\code{gravity(q)}, \code{forward_accel(q, qd, tau)}, \code{inverse_dynamics(q, qd, qdd)},
\code{energy(q, qd)} (kinetic, potential, total), and \code{ndof = 2}.
}
\description{
Builds the analytic dynamics of a planar two-link limb with distributed
segment mass: the mass matrix \code{M(q)}, the Coriolis/centrifugal matrix, the
gravity vector, and forward/inverse dynamics. Angles are joint angles
(proximal, then distal relative to the proximal segment), measured so gravity
acts in \code{-y}.
}
\examples{
m <- planarLimbModel(mass = c(7, 3), length = c(0.4, 0.4))
m$energy(c(0, 0), c(1, 0))$total
}
\references{
Spong MW, et al. (2006) Robot Modeling and Control.
}
\seealso{
\code{\link[=forwardDynamics]{forwardDynamics()}}, \code{\link[=predictiveSimulation]{predictiveSimulation()}}
}
