% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-gaitrec.R
\name{readGaitRec}
\alias{readGaitRec}
\title{Read GaitRec Dataset}
\usage{
readGaitRec(
  path,
  type = c("grf", "parameters"),
  sr = 1000,
  sep = ",",
  subject_id = NULL
)
}
\arguments{
\item{path}{Path to a GaitRec CSV file or a directory containing GaitRec
CSV files. When a directory is given, all CSV files matching the specified
\code{type} are read and combined.}

\item{type}{Type of data to read: \code{"grf"} for ground reaction force
waveforms or \code{"parameters"} for spatiotemporal parameters. Default
\code{"grf"}.}

\item{sr}{Sampling rate in Hz. Default \code{1000} for GaitRec GRF data. For
time-normalised waveforms (101 data points representing 0--100\\% of the
gait cycle), this value is used as a nominal sampling rate in the
resulting PhysioExperiment object.}

\item{sep}{Column separator in the CSV file. Default \code{","}. Set to
\code{"\\t"} for tab-separated files.}

\item{subject_id}{Optional subject identifier string to store in the
returned object's metadata. When \code{path} is a directory this is ignored
and identifiers are extracted from file names.}
}
\value{
For \code{type = "grf"}: a \code{PhysioExperiment} object with a \code{"raw"}
assay containing the GRF data matrix (time points x channels). The
\code{colData} records channel labels, force component (\code{"Fx"}, \code{"Fy"},
\code{"Fz"}), plate number, and unit (\code{"N"}). Metadata includes
\code{source_file}, \code{format}, and \code{subject_id}.

For \code{type = "parameters"}: a \code{data.frame} with spatiotemporal gait
parameters extracted from the CSV file.
}
\description{
Reads ground reaction force (GRF) data or spatiotemporal parameters from
the GaitRec dataset format (Horsak et al., 2020). GaitRec is a large-scale
clinical gait dataset containing GRF waveforms and gait parameters from
patients with various lower-limb orthopaedic conditions and healthy controls.
}
\details{
\subsection{GaitRec GRF file format}{

GaitRec GRF CSV files typically contain columns for time and force
components from two force plates:
\itemize{
\item A time column (e.g., \code{"time"}, \code{"Time"}, \code{"t"})
\item Force columns following naming patterns like \code{"Fx1"}, \code{"Fy1"},
\code{"Fz1"}, \code{"Fx2"}, \code{"Fy2"}, \code{"Fz2"} (plate number suffix) or
\code{"FP1_Fx"}, \code{"FP1_Fy"}, \code{"FP1_Fz"}, \code{"FP2_Fx"}, etc. (plate prefix)
}

The function auto-detects force column naming patterns and extracts the
component (x/y/z) and plate number. Columns that do not match recognised
force patterns are stored in \code{metadata(pe)$extra_columns} as a
data.frame.

Time-normalised waveforms (101 data points, 0--100\\% gait cycle) are
also supported. The parser detects these by row count and stores a
\code{percent_gait_cycle} vector in the metadata.
}
}
\examples{
\dontrun{
# Read a single GRF file
pe <- readGaitRec("path/to/gaitrec_grf.csv")

# Read spatiotemporal parameters
params <- readGaitRec("path/to/gaitrec_params.csv", type = "parameters")

# Read with tab separator
pe <- readGaitRec("path/to/gaitrec.tsv", sep = "\t")
}
}
\references{
Horsak B, Slijepcevic D, Raberger A-M, Schwab C, Worisch M, Zeppelzauer M
(2020). "GaitRec, a large-scale ground reaction force dataset of healthy
and impaired gait." \emph{Scientific Data}, 7, 143.
\doi{10.1038/s41597-020-0481-z}
}
\seealso{
\code{\link[=readMoCapCSV]{readMoCapCSV()}} for generic CSV motion capture data,
\code{\link[=filterGRF]{filterGRF()}} for low-pass filtering GRF signals,
\code{\link[=analyzeForcePlate]{analyzeForcePlate()}} for force plate analysis.
}
