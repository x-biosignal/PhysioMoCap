% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user-onboarding.R
\name{quickStartMoCap}
\alias{quickStartMoCap}
\title{Run a one-command beginner workflow}
\usage{
quickStartMoCap(
  n_frames = 300,
  sampling_rate = NULL,
  emg_sampling_rate = 1000,
  seed = 123,
  mocap = NULL,
  path = NULL,
  format = c("auto", "csv", "c3d", "trc", "bvh", "amc"),
  forces = NULL,
  joints = NULL,
  joint_angles = NULL,
  emg = NULL
)
}
\arguments{
\item{n_frames}{Number of MoCap frames for demo mode.}

\item{sampling_rate}{MoCap sampling rate in Hz. In non-demo mode, if \code{NULL},
uses \code{samplingRate(mocap)}.}

\item{emg_sampling_rate}{EMG sampling rate in Hz.}

\item{seed}{Random seed for reproducibility in demo mode.}

\item{mocap}{Optional \code{PhysioExperiment} object to analyze.}

\item{path}{Optional file path to load via \code{readMoCapAuto()}.}

\item{format}{Format hint passed to \code{readMoCapAuto()} when \code{path} is used.}

\item{forces}{Optional force matrix/data.frame for force-plate analysis.}

\item{joints}{Optional joint-center data for inverse dynamics.}

\item{joint_angles}{Optional joint-angle data for inverse dynamics.}

\item{emg}{Optional EMG matrix for EMG processing.}
}
\value{
An object of class \code{"mocap_quickstart"} containing generated outputs,
readiness report, and notes for skipped/failed optional modules.
}
\description{
Runs a compact end-to-end pipeline for first-time users:
kinematics derivatives, readiness scoring, and optional force-plate,
inverse-dynamics, and EMG modules.
}
\details{
If \code{mocap} or \code{path} is omitted, synthetic demo data are generated.
}
\examples{
qs <- quickStartMoCap(seed = 1)
qs
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=demoMoCapData]{demoMoCapData()}} for generating demo data without analysis,
\code{\link[=assessMoCapReadiness]{assessMoCapReadiness()}} for data quality assessment,
\code{\link[=print.mocap_quickstart]{print.mocap_quickstart()}} for formatted display of results.
}
