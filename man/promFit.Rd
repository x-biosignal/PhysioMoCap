% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motor-primitives.R
\name{promFit}
\alias{promFit}
\title{Fit a Probabilistic Movement Primitive to demonstrations}
\usage{
promFit(demos, n_basis = 15L, ridge = 1e-06)
}
\arguments{
\item{demos}{A list of demonstrations (each a numeric vector, all resampled to
the same length), or an \verb{N x T} matrix (one demo per row).}

\item{n_basis}{Number of Gaussian basis functions (default 15).}

\item{ridge}{Ridge penalty for the per-demo weight fit (default 1e-6).}
}
\value{
a \code{promp} object: \code{mean} and \code{sd} trajectories, the weight mean
\code{w_mean} and covariance \code{w_cov}, the basis \code{Phi}, and phase \code{z}.
}
\description{
Learns a distribution over trajectories from several demonstrations: each is
projected onto a Gaussian basis, and the basis weights are modelled as
Gaussian, giving a mean trajectory and a variability band.
}
\examples{
z <- seq(0, 1, length.out = 50)
demos <- lapply(1:20, function(i) sin(2 * pi * z) + rnorm(50, 0, 0.1))
p <- promFit(demos)
length(p$mean)                                     # mean trajectory
}
\references{
Paraschos A, et al. (2013) NIPS 26.
}
\seealso{
\code{\link[=promCondition]{promCondition()}}, \code{\link[=dmpFit]{dmpFit()}}
}
