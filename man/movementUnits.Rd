% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reaching.R
\name{movementUnits}
\alias{movementUnits}
\title{Count reaching movement units}
\usage{
movementUnits(
  speed,
  fs = NULL,
  height_frac = 0.05,
  prominence_frac = 0.1,
  min_peak_distance = NULL
)
}
\arguments{
\item{speed}{Numeric non-negative tangential-speed profile.}

\item{fs}{Optional sampling frequency in Hz. Required only when
\code{min_peak_distance} is supplied.}

\item{height_frac}{Peak-height threshold as a fraction of maximum speed.}

\item{prominence_frac}{Minimum peak prominence as a fraction of maximum
speed.}

\item{min_peak_distance}{Optional minimum inter-peak interval in seconds.}
}
\value{
Integer movement-unit count. Accepted 1-based peak indices are
stored in the \code{"peaks"} attribute.
}
\description{
Counts accepted speed peaks as movement units (submovements). A peak must
exceed both a relative height threshold and a relative prominence threshold.
}
\examples{
t <- seq(0, 1, length.out = 500)
speed <- exp(-((t - 0.3) / 0.04)^2) + exp(-((t - 0.7) / 0.04)^2)
movementUnits(speed)
}
\references{
Rohrer B, Fasoli S, Krebs HI, et al. (2002). Movement smoothness changes
during stroke recovery. \emph{Journal of Neuroscience}, 22:8297-8304.
}
