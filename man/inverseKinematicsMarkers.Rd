% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inverse-kinematics.R
\name{inverseKinematicsMarkers}
\alias{inverseKinematicsMarkers}
\title{Model-based inverse kinematics from markers (planar)}
\usage{
inverseKinematicsMarkers(markers, model, anchor_root = TRUE)
}
\arguments{
\item{markers}{A \verb{[frame, node, 2]} array of observed node positions (root and
each joint/end), or a list of \verb{(n_segments + 1) x 2} matrices.}

\item{model}{A \code{planar_ik_model} from \code{\link[=scalePlanarModel]{scalePlanarModel()}}.}

\item{anchor_root}{If \code{TRUE} (default) the root is fixed to the observed root
marker each frame; otherwise the root is also fitted.}
}
\value{
an \code{ik_result}: \code{angles} (\verb{frame x n_segments}, absolute radians),
\code{rmse} (per-frame marker RMS error, in position units), and \code{n_frames}.
}
\description{
Solves the segment angles of a scaled planar chain that best fit observed
marker positions, frame by frame, by least squares. Because the segment
lengths are fixed by the model, the solution is consistent (unlike reading
angles directly off noisy markers, which lets the limb stretch).
}
\examples{
model <- scalePlanarModel(rbind(c(0, 0), c(1, 0), c(2, 0)))
truth <- forwardKinematics2D(c(0.3, 0.7), model$lengths)
arr <- array(0, c(1, 3, 2)); arr[1, , ] <- truth
inverseKinematicsMarkers(arr, model)$angles         # ~ c(0.3, 0.7)
}
\references{
Lu TW, O'Connor JJ (1999) global optimisation IK, J Biomech
32:129-134.
}
\seealso{
\code{\link[=scalePlanarModel]{scalePlanarModel()}}, \code{\link[=forwardKinematics2D]{forwardKinematics2D()}}
}
