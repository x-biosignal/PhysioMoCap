% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adl-reach.R
\name{nhptDexterity}
\alias{nhptDexterity}
\title{Instrumented Nine-Hole-Peg dexterity from a hand-speed profile}
\usage{
nhptDexterity(speed, sampling_rate, ...)
}
\arguments{
\item{speed}{Numeric hand speed profile over the whole task.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{...}{Passed to \code{\link[=movementUnits]{movementUnits()}} (peak height/prominence controls).}
}
\value{
an \code{nhpt_dexterity} list: \code{icf_code}, \code{n_transports}, \code{total_time},
\code{transport_rate} (transports/s), \code{mean_interval}, \code{cv_interval} (interval
consistency) and \code{smoothness} (SPARC).
}
\description{
Turns the hand-speed profile of a peg-transport task (the Nine-Hole-Peg Test
of fine hand use) into instrumented dexterity metrics: the number of detected
peg transports (speed peaks), the transport rate, the mean and coefficient of
variation of the inter-transport intervals (movement consistency) and the
overall smoothness. This is the sensor-derived complement to the clinical
timed NHPT score, and realises ICF \code{d440} (fine hand use).
}
\examples{
fs <- 100
bell <- function(n) { u <- seq(0, 1, length.out = n); 30 * u^2 * (1 - u)^2 }
# nine evenly spaced transports
v <- unlist(replicate(9, c(bell(40), numeric(20)), simplify = FALSE))
nhptDexterity(v, fs)$n_transports
}
\references{
Mathiowetz V, et al. (1985) Am J Occup Ther 39:386-391 (NHPT).
}
\seealso{
\code{\link[=movementUnits]{movementUnits()}}, \code{\link[=adlReachTask]{adlReachTask()}}
}
