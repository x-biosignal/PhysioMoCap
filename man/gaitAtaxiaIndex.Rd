% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ataxia.R
\name{gaitAtaxiaIndex}
\alias{gaitAtaxiaIndex}
\title{Gait-ataxia composite from gait-variability sub-metrics}
\usage{
gaitAtaxiaIndex(
  step_width_cv = NA_real_,
  stride_length_cv = NA_real_,
  stride_time_cv = NA_real_,
  trunk_sway = NA_real_,
  reference = NULL,
  weights = NULL
)
}
\arguments{
\item{step_width_cv, stride_length_cv, stride_time_cv}{Gait coefficients of
variation (per cent); any may be \code{NA}.}

\item{trunk_sway}{Trunk-sway magnitude (higher = worse); optional.}

\item{reference, weights}{As in \code{\link[=limbAtaxiaIndex]{limbAtaxiaIndex()}}.}
}
\value{
An S3 \code{gait_ataxia} list: \code{metrics}, per-metric \code{z}, and \code{composite}.
}
\description{
Aggregates the standard gait-ataxia sub-metrics (each higher = more ataxic):
the coefficients of variation of step width, stride length and stride time
(from \code{\link[=summarizeGaitParameters]{summarizeGaitParameters()}}, the \code{cv} column, in per cent) and trunk
sway (e.g. \code{cop_path_length} from \code{\link[=swayMetrics]{swayMetrics()}}, or trunk-marker RMS).
}
\examples{
gaitAtaxiaIndex(step_width_cv = 28, stride_length_cv = 9, stride_time_cv = 6)
}
\seealso{
\code{\link[=summarizeGaitParameters]{summarizeGaitParameters()}}, \code{\link[=swayMetrics]{swayMetrics()}}, \code{\link[=limbAtaxiaIndex]{limbAtaxiaIndex()}}
}
