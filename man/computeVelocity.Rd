% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-derivatives.R
\name{computeVelocity}
\alias{computeVelocity}
\title{Compute velocity from position data}
\usage{
computeVelocity(
  pe,
  assay_names = NULL,
  method = "central",
  sampling_rate = NULL
)
}
\arguments{
\item{pe}{A PhysioExperiment object containing position assays.}

\item{assay_names}{Character vector of assay names to differentiate.
If NULL (default), auto-detects position_x/y/z or keypoint_x/y assays.}

\item{method}{Finite difference method: "central" (default), "forward",
or "backward".}

\item{sampling_rate}{Sampling rate in Hz. If NULL (default), uses
\code{samplingRate(pe)}.}
}
\value{
PhysioExperiment with new velocity assays added. For 3D marker data,
assays named velocity_x, velocity_y, velocity_z. For 2D keypoint data,
assays named velocity_kp_x, velocity_kp_y.
}
\description{
Computes velocity (first derivative) from position assays in a
PhysioExperiment object using finite differences.
}
\examples{
\dontrun{
pe <- make_mocap_markers(n_time = 100, n_markers = 4, sr = 120)
pe <- computeVelocity(pe)
# velocity_x, velocity_y, velocity_z assays are now present
}
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=computeAcceleration]{computeAcceleration()}} for second-order derivatives,
\code{\link[=computeSpeed]{computeSpeed()}} for scalar speed from velocity components,
\code{\link[=differentiate]{differentiate()}} for general-purpose numerical differentiation.
}
