% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-TaskSchema.R
\name{TaskSchema}
\alias{TaskSchema}
\title{Create a Task Schema}
\usage{
TaskSchema(
  task_type,
  task_label = task_type,
  events = list(),
  phases = list(),
  normalization = c("cycle", "phase", "landmark", "dtw", "absolute"),
  norm_length = 101L,
  metrics = character(),
  vis_defaults = list()
)
}
\arguments{
\item{task_type}{Short identifier for the task type (e.g., "gait", "jump")}

\item{task_label}{Human-readable label (e.g., "Gait Cycle", "Vertical Jump")}

\item{events}{List of Event objects defining key timepoints}

\item{phases}{List of Phase objects defining movement phases}

\item{normalization}{Normalization method:
\itemize{
\item "cycle" - Normalize entire movement to 0-100\%
\item "phase" - Normalize each phase independently to 0-100\%
\item "landmark" - Align to a specific event
\item "dtw" - Dynamic time warping alignment
\item "absolute" - Keep original time (no normalization)
}}

\item{norm_length}{Target length after normalization (default 101 for 0-100\%)}

\item{metrics}{Character vector of recommended metrics for this task}

\item{vis_defaults}{List of default visualization parameters:
\itemize{
\item xlab - X-axis label
\item ylab - Y-axis label
\item show_phases - Whether to show phase regions
\item show_events - Whether to show event markers
\item phase_alpha - Transparency for phase shading
\item landmark_event - Event to align to (for landmark normalization)
}}
}
\value{
A TaskSchema object (S3 class)
}
\description{
Defines a complete movement task schema including events, phases,
normalization method, metrics, and visualization defaults.
}
\examples{
# Simple task schema
my_task <- TaskSchema(
  task_type = "simple",
  task_label = "Simple Movement",
  events = list(
    Event("start", "Start", "threshold",
          list(signal = "velocity", threshold = 0.1, direction = "rising")),
    Event("end", "End", "threshold",
          list(signal = "velocity", threshold = 0.1, direction = "falling"))
  ),
  phases = list(
    Phase("main", "Main Phase", "start", "end")
  ),
  normalization = "cycle"
)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. Wiley.
}
\seealso{
\code{\link[=Event]{Event()}}, \code{\link[=Phase]{Phase()}}, \code{\link[=getSchema]{getSchema()}}, \code{\link[=validateSchema]{validateSchema()}}
}
