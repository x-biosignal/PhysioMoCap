% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mocap-angles.R
\name{quaternionToEuler}
\alias{quaternionToEuler}
\title{Convert quaternion to Euler angles}
\usage{
quaternionToEuler(w, x, y, z, order = "ZYX", degrees = TRUE)
}
\arguments{
\item{w}{Numeric. Scalar (real) part of the quaternion.}

\item{x}{Numeric. First imaginary component.}

\item{y}{Numeric. Second imaginary component.}

\item{z}{Numeric. Third imaginary component.}

\item{order}{Character. Rotation order. Default \code{"ZYX"}.}

\item{degrees}{Logical. If \code{TRUE} (default), return Euler angles
in degrees. If \code{FALSE}, return in radians.}
}
\value{
A matrix with columns \code{roll}, \code{pitch}, \code{yaw}. If inputs
are scalars, returns a 1-row matrix. If inputs are vectors,
returns a matrix with one row per element.
}
\description{
Converts a quaternion (w, x, y, z) representation to Euler angles
(roll, pitch, yaw) using the specified rotation order.
}
\details{
For the \code{"ZYX"} (Tait-Bryan) convention:
\itemize{
\item roll = rotation about X axis
\item pitch = rotation about Y axis
\item yaw = rotation about Z axis
}

The quaternion is assumed to be unit (normalized). If not unit,
it is normalized internally before conversion.
}
\examples{
# Identity quaternion -> zero Euler angles
quaternionToEuler(1, 0, 0, 0)

# 90-degree rotation about Z axis
quaternionToEuler(cos(pi/4), 0, 0, sin(pi/4))
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. Wiley.

Grood ES, Suntay WJ (1983). "A joint coordinate system for the clinical
description of three-dimensional motions: application to the knee."
Journal of Biomechanical Engineering, 105(2), 136-144.
}
\seealso{
\code{\link[=eulerToQuaternion]{eulerToQuaternion()}}, \code{\link[=calculateJointAngles]{calculateJointAngles()}}, \code{\link[=vectorAngle]{vectorAngle()}}
}
