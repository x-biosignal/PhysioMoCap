% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mocap-angles.R
\name{vectorAngle}
\alias{vectorAngle}
\title{Angle between two 3D vectors}
\usage{
vectorAngle(v1, v2, degrees = TRUE)
}
\arguments{
\item{v1}{Numeric vector of length 3 or matrix with 3 columns.}

\item{v2}{Numeric vector of length 3 or matrix with 3 columns.}

\item{degrees}{Logical. If \code{TRUE} (default), return angles in
degrees. If \code{FALSE}, return in radians.}
}
\value{
Numeric vector of angles. Length 1 for vector inputs,
or \code{nrow(v1)} for matrix inputs.
}
\description{
Computes the angle between pairs of 3D vectors. Accepts either
single vectors (length-3 numeric) or matrices where each row is a
vector (n x 3). Vectorized for efficient computation across time
frames.
}
\details{
The angle is computed as:
\deqn{\theta = \arccos\left(\frac{v_1 \cdot v_2}{|v_1| |v_2|}\right)}

The dot product is clamped to \eqn{[-1, 1]} before applying
\code{acos} to avoid numerical issues. If either vector has zero
magnitude, the result is \code{NA}.

The result is unsigned, in \eqn{[0, 180]}: it says how far apart the two
vectors are but not which side of \code{v1} that \code{v2} lies on, so mirror-image
configurations (joint flexion versus hyperextension) are indistinguishable.
For the signed variant, measured about a plane normal and therefore able to
separate the two, use \code{calculateJointAngles(..., signed = TRUE)}.
}
\examples{
# 90-degree angle
vectorAngle(c(1, 0, 0), c(0, 1, 0))

# Vectorized across rows
v1 <- matrix(c(1,0,0, 0,1,0), nrow = 2, byrow = TRUE)
v2 <- matrix(c(0,1,0, 0,0,1), nrow = 2, byrow = TRUE)
vectorAngle(v1, v2)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. Wiley.

Grood ES, Suntay WJ (1983). "A joint coordinate system for the clinical
description of three-dimensional motions: application to the knee."
Journal of Biomechanical Engineering, 105(2), 136-144.
}
\seealso{
\code{\link[=calculateJointAngles]{calculateJointAngles()}}, \code{\link[=quaternionToEuler]{quaternionToEuler()}}, \code{\link[=eulerToQuaternion]{eulerToQuaternion()}}
}
