% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motor-variability-ucm.R
\name{goalEquivalentManifold}
\alias{goalEquivalentManifold}
\title{Goal-Equivalent Manifold (GEM) decomposition for a scalar goal}
\usage{
goalEquivalentManifold(execution, goal_gradient = NULL, goal = NULL)
}
\arguments{
\item{execution}{An \verb{N x m} matrix of execution/body variables across trials.}

\item{goal_gradient}{Length-\code{m} gradient of the goal function at the mean (the
direction in which the goal changes fastest). Provide this, or \code{goal}.}

\item{goal}{Optional scalar goal function \code{R^m -> R}; its gradient is computed
numerically at the mean when \code{goal_gradient} is not given.}
}
\value{
a \code{gem_result} list: \code{gev} (goal-equivalent variance, along the GEM),
\code{ngev} (non-goal-equivalent variance, along the gradient), \code{me_ratio}
(\code{gev / ngev}, > 1 = variability channelled into the goal-irrelevant
direction), \code{log_me_ratio}.
}
\description{
The scalar-goal special case of UCM (Cusumano & Cesari 2006): given the
gradient of a scalar goal function, split the execution variability into
goal-equivalent (along the level set -- does not change the goal) and
non-goal-equivalent (along the gradient -- changes it), and report the
motor-equivalent ratio.
}
\examples{
set.seed(2)
x1 <- rnorm(200, 10, 3); x2 <- 20 - x1 + rnorm(200, 0, 0.4)  # x1+x2 ~ const
goalEquivalentManifold(cbind(x1, x2), goal_gradient = c(1, 1))$me_ratio
}
\references{
Cusumano JP, Cesari P (2006) Biol Cybern 94:367-379.
}
\seealso{
\code{\link[=uncontrolledManifold]{uncontrolledManifold()}}
}
