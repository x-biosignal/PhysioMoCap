% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-inverse-dynamics.R
\name{labelPowerBursts}
\alias{labelPowerBursts}
\title{Label joint power bursts with Winter nomenclature}
\usage{
labelPowerBursts(
  power,
  sampling_rate,
  joint = c("ankle", "knee", "hip"),
  body_mass = NULL
)
}
\arguments{
\item{power}{Numeric vector of joint power (W) over one gait cycle, or a data
frame with a \code{power} column.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{joint}{One of \code{"ankle"}, \code{"knee"}, or \code{"hip"}.}

\item{body_mass}{Optional body mass in kilograms; if supplied, a \code{work_per_kg}
column (J/kg) is added.}
}
\value{
A data frame (class \code{power_bursts}) with one row per detected burst:
\code{label} (canonical burst name or \code{NA} if unmatched), \code{joint}, \code{type}
(\code{"generation"} or \code{"absorption"}), \code{start_pct}, \code{end_pct}, \code{peak_power}
(W, signed), and \code{work} (J), ordered by time.
}
\description{
Segments a single-gait-cycle joint power time series into contiguous
same-sign bursts and assigns Winter's power-phase labels: \code{A1}/\code{A2} for the
ankle, \code{H1}-\code{H3} for the hip, and \code{K1}-\code{K4} for the knee. Each canonical
burst has an expected sign (generation vs absorption) and an approximate
location within the gait cycle; detected bursts are matched to the nearest
unused canonical burst of the same sign.
}
\details{
The \code{power} vector is assumed to span exactly one gait cycle (initial contact
to the next ipsilateral initial contact), so burst timing is reported as a
percentage of the cycle.
}
\examples{
t <- seq(0, 1, length.out = 101)
ankle_power <- ifelse(t < 0.4, -50 * sin(pi * t / 0.4),
                      120 * sin(pi * (t - 0.4) / 0.2) * (t < 0.6))
labelPowerBursts(ankle_power, sampling_rate = 100, joint = "ankle")
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons. Power-phase nomenclature A1-A2 / H1-H3 / K1-K4.
}
\seealso{
\code{\link[=computeJointPower]{computeJointPower()}}, \code{\link[=jointWork]{jointWork()}}.
}
