% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clinical-mobility-tests.R
\name{instrumentedTUG}
\alias{instrumentedTUG}
\title{Instrumented Timed Up-and-Go (iTUG)}
\usage{
instrumentedTUG(
  angular_velocity,
  sampling_rate,
  turn_axis = 3L,
  transition_axis = 2L,
  turn_threshold = NULL,
  min_turn_sec = 0.3
)
}
\arguments{
\item{angular_velocity}{An n x 3 matrix of trunk angular velocity (rad/s).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{turn_axis}{Column index of the vertical/yaw axis (default 3).}

\item{transition_axis}{Column index of the trunk flexion/pitch axis
(default 2).}

\item{turn_threshold}{Angular-velocity threshold (rad/s) that marks a turn;
\code{NULL} uses \code{0.15 * max(abs(yaw))}.}

\item{min_turn_sec}{Minimum turn duration in seconds (default 0.3).}
}
\value{
An \code{itug_report} object with \code{total_duration}, a \code{turns} data frame
(\code{start}, \code{end}, \code{duration}, \code{peak_velocity}, \code{angle_deg}), the
\code{stand_up_time}/\code{sit_down_time}, and a \code{phases} data frame.
}
\description{
Segments a Timed Up-and-Go trial from a trunk/lumbar inertial sensor into its
sub-phases (sit-to-stand, walk, turn, turn-to-sit) using the angular velocity:
turns appear as large excursions of the vertical-axis (yaw) angular velocity,
and the postural sit-to-stand / turn-to-sit transitions as the trunk
flexion-axis (pitch) events that bracket the walking (Salarian et al. 2010).
}
\references{
Salarian A, et al. (2010). IEEE Trans Neural Syst Rehabil Eng
18(3):303-310.
}
\seealso{
\code{\link[=instrumented10mWT]{instrumented10mWT()}}
}
