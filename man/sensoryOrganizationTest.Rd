% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posturography.R
\name{sensoryOrganizationTest}
\alias{sensoryOrganizationTest}
\title{Sensory Organization Test (SOT) equilibrium and sensory-ratio scoring}
\usage{
sensoryOrganizationTest(
  scores = NULL,
  sway_angle = NULL,
  theta_limit = 12.5,
  fall_as_zero = TRUE
)
}
\arguments{
\item{scores}{A 6-row (conditions 1-6) numeric matrix / \code{data.frame} of
equilibrium scores, columns = trials; or a list of 6 numeric vectors. \code{NA}
entries (e.g. a fall) are treated as a fall with score 0 when
\code{fall_as_zero = TRUE}.}

\item{sway_angle}{Alternatively, peak-to-peak AP sway angles (degrees) in the
same shape as \code{scores}; converted to equilibrium scores.}

\item{theta_limit}{Theoretical AP stability limit in degrees (default 12.5).}

\item{fall_as_zero}{Logical; treat \code{NA} trials as falls scored 0
(default \code{TRUE}).}
}
\value{
A \code{sot_result} object with per-trial \code{equilibrium}, per-condition
\code{condition_means}, \code{composite}, and \code{ratios} (SOM/VIS/VEST/PREF).
}
\description{
Computes NeuroCom-style Sensory Organization Test equilibrium scores,
composite score, and the four sensory-analysis ratios from either per-trial
equilibrium scores or per-trial peak-to-peak anteroposterior sway angles.
}
\details{
The equilibrium score for a trial is
\eqn{EQ = 100 \times (\theta_{lim} - \theta_{pp}) / \theta_{lim}}, where
\eqn{\theta_{pp}} is the peak-to-peak AP sway angle and \eqn{\theta_{lim}} the
theoretical AP stability limit (12.5 deg). The composite is the mean of the
condition-1 and condition-2 average scores together with every individual
trial score of conditions 3-6. The sensory ratios use condition average
scores: SOM = C2/C1, VIS = C4/C1, VEST = C5/C1, PREF = (C3+C6)/(C2+C5).
}
\examples{
# Three trials per condition, equilibrium scores already computed
sc <- rbind(c(94, 95, 93), c(92, 91, 93), c(88, 90, 89),
            c(85, 84, 86), c(70, 72, 68), c(65, 66, 64))
sensoryOrganizationTest(sc)
}
\references{
Nashner LM (1997). "Computerized dynamic posturography."
}
\seealso{
\code{\link[=mCTSIB]{mCTSIB()}}, \code{\link[=limitsOfStability]{limitsOfStability()}}, \code{\link[=swayMetrics]{swayMetrics()}}
}
