% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motor-variability-ucm.R
\name{uncontrolledManifold}
\alias{uncontrolledManifold}
\title{Uncontrolled Manifold (UCM) analysis of motor variability}
\usage{
uncontrolledManifold(theta, jacobian = NULL, task = NULL)
}
\arguments{
\item{theta}{An \verb{N x n} matrix: \code{N} trials (repetitions), \code{n} elemental
variables (e.g. joint angles).}

\item{jacobian}{The \verb{d x n} task Jacobian at the mean configuration (\code{d} =
dimension of the task variable). Provide this, or \code{task}.}

\item{task}{Optional task function \code{R^n -> R^d}; its Jacobian is computed
numerically at \code{colMeans(theta)} when \code{jacobian} is not given.}
}
\value{
a \code{ucm_result} list: \code{v_ucm}, \code{v_ort} (variance per DOF, parallel and
orthogonal to the manifold), \code{v_total}, \code{delta_v} (synergy index,
\code{(v_ucm - v_ort) / v_total}; > 0 = task-stabilising synergy), \code{n}, \code{d}, \code{N}.
}
\description{
Decomposes trial-to-trial variance of the elemental variables into the part
lying in the uncontrolled manifold (the null space of the task Jacobian --
variability that does not change the task variable, "good") and the part
orthogonal to it (variability that does, "bad"). A positive synergy index
means the elements co-vary to stabilise the task.
}
\examples{
set.seed(1)
# redundant task: total = a1 + a2 held constant; variance mostly along the UCM
a1 <- rnorm(200, 30, 4); a2 <- 60 - a1 + rnorm(200, 0, 0.5)
uncontrolledManifold(cbind(a1, a2), jacobian = matrix(c(1, 1), nrow = 1))$delta_v
}
\references{
Scholz JP, Schoner G (1999) Exp Brain Res 126:289-306; Latash ML,
et al. (2002) Exerc Sport Sci Rev 30:26-31.
}
\seealso{
\code{\link[=goalEquivalentManifold]{goalEquivalentManifold()}}, \code{\link[=toleranceNoiseCovariation]{toleranceNoiseCovariation()}}
}
