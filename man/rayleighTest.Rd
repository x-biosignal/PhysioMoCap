% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-circular.R
\name{rayleighTest}
\alias{rayleighTest}
\title{Rayleigh test for a preferred direction}
\usage{
rayleighTest(angles, units = c("degrees", "radians"))
}
\arguments{
\item{angles}{Numeric vector of angles.}

\item{units}{\code{"degrees"} (default) or \code{"radians"}.}
}
\value{
an \code{htest}: Rayleigh \code{Z = n * rbar^2}, \code{p.value} (Zar 1999
approximation), and the estimated mean direction.
}
\description{
Tests the null hypothesis that the angles are uniformly distributed on the
circle (no preferred direction) against a unimodal concentration. A small
p-value means the angles cluster around a mean direction -- e.g. that a
coupling angle is consistently in-phase rather than random.
}
\examples{
set.seed(1)
rayleighTest(rnorm(40, mean = 30, sd = 12))$p.value   # clustered -> small
}
\references{
Zar JH (1999) eq. 27.2-27.4; Fisher NI (1993).
}
\seealso{
\code{\link[=circularSummary]{circularSummary()}}, \code{\link[=watsonWilliamsTest]{watsonWilliamsTest()}}
}
