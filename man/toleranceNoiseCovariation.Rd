% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motor-variability-ucm.R
\name{toleranceNoiseCovariation}
\alias{toleranceNoiseCovariation}
\title{Tolerance-Noise-Covariation (TNC) decomposition of result error}
\usage{
toleranceNoiseCovariation(
  execution,
  error_fn,
  optimum = NULL,
  n_surrogate = 200L
)
}
\arguments{
\item{execution}{An \verb{N x m} matrix of execution variables.}

\item{error_fn}{A vectorised error function: given an \verb{N x m} matrix it
returns the length-\code{N} per-trial error (>= 0, 0 = perfect).}

\item{optimum}{Length-\code{m} execution vector achieving (near-)zero error, used
as the most tolerant target. When \code{NULL}, the dispersion (noise) reference
is the cloud centroid.}

\item{n_surrogate}{Number of column-permutation surrogates for the covariation
estimate (default 200).}
}
\value{
a \code{tnc_result} list: \code{tolerance}, \code{noise}, \code{covariation} (error
components; covariation > 0 = the observed covariation reduces error),
\code{e_data} (mean data error), \code{e_optimum}.
}
\description{
Decomposes the mean result error of a set of executions (Muller & Sternad
2004): how much error is removable by moving the mean to a more \strong{tolerant}
region, how much is due to dispersion (\strong{noise}) around that region, and how
much the observed inter-variable \strong{covariation} already saves relative to a
de-covaried (column-permuted) surrogate.
}
\examples{
set.seed(3)
# redundant reaching task: result = x1 + x2, target 20, error squared
x1 <- rnorm(300, 12, 3); x2 <- 20 - x1 + rnorm(300, 0, 0.6)   # covary to hit 20
err <- function(M) (M[, 1] + M[, 2] - 20)^2
toleranceNoiseCovariation(cbind(x1, x2), err, optimum = c(10, 10))$covariation
}
\references{
Muller H, Sternad D (2004) J Exp Psychol Hum Percept Perform
30:212-233; Cohen RG, Sternad D (2009) Exp Brain Res 193:69-83.
}
\seealso{
\code{\link[=uncontrolledManifold]{uncontrolledManifold()}}
}
