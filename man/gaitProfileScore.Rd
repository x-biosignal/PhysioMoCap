% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gait-indices.R
\name{gaitProfileScore}
\alias{gaitProfileScore}
\title{Gait Profile Score (overall RMS deviation from the norm)}
\usage{
gaitProfileScore(kinematics, norm = NULL)
}
\arguments{
\item{kinematics}{Subject kinematics: a numeric matrix with one row per
kinematic variable (row names matching \code{norm$variables}) and one column per
gait-cycle point, or a named list of per-variable curves. Curves are
time-normalised to the norm's cycle length via \code{\link[=normalizeMovement]{normalizeMovement()}}.}

\item{norm}{A \code{gait_norm} normative reference (from
\code{PhysioGaitNorm::loadGaitNorm()}); if \code{NULL}, the default reference is
loaded from PhysioGaitNorm.}
}
\value{
A single non-negative numeric (degrees).
}
\description{
The Gait Profile Score (Baker et al. 2009) summarises overall gait deviation
as the root-mean-square of the Gait Variable Scores across all kinematic
variables (degrees). It is always non-negative and is zero when the subject
equals the normative mean.
}
\examples{
norm <- list(variables = c("a", "b"),
             mean = matrix(0, 2, 51, dimnames = list(c("a", "b"), NULL)),
             cycle_length = 51)
gaitProfileScore(matrix(1, 2, 51, dimnames = list(c("a", "b"), NULL)), norm)
}
\references{
Baker R, et al. (2009). Gait & Posture 30(3):265-269.
}
\seealso{
\code{\link[=gaitVariableScore]{gaitVariableScore()}}, \code{\link[=movementAnalysisProfile]{movementAnalysisProfile()}}
}
