% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-orbital-rqa.R
\name{recurrenceQuantification}
\alias{recurrenceQuantification}
\title{Recurrence quantification analysis (RQA)}
\usage{
recurrenceQuantification(
  x,
  m = 3L,
  tau = 1L,
  radius = NULL,
  target_rr = 0.1,
  lmin = 2L,
  vmin = 2L,
  max_n = 1500L
)
}
\arguments{
\item{x}{Numeric time series.}

\item{m}{Embedding dimension (default 3).}

\item{tau}{Embedding delay (default 1).}

\item{radius}{Recurrence threshold; if \code{NULL}, chosen to reach \code{target_rr}.}

\item{target_rr}{Target recurrence rate when \code{radius} is \code{NULL} (default 0.1).}

\item{lmin, vmin}{Minimum diagonal / vertical line lengths (default 2).}

\item{max_n}{Cap on embedded points (subsampled if longer; default 1500).}
}
\value{
an \code{rqa_result}: \code{RR}, \code{DET}, \code{LAM}, \code{Lmax}, \code{Lmean}, \code{TT} (trapping
time), \code{ENTR} (diagonal line entropy), \code{radius}.
}
\description{
Reconstructs the attractor by delay embedding and quantifies its recurrence
structure: recurrence rate, determinism, laminarity and diagonal/vertical
line statistics. High determinism indicates deterministic (periodic/chaotic)
rather than stochastic dynamics.
}
\examples{
t <- seq(0, 20 * pi, length.out = 800)
recurrenceQuantification(sin(t))$DET               # ~1 (deterministic)
}
\references{
Marwan N, et al. (2007) Phys Rep 438:237-329.
}
\seealso{
\code{\link[=floquetMultipliers]{floquetMultipliers()}}, \code{\link[=approximateEntropy]{approximateEntropy()}}
}
