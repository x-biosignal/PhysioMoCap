% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-nonlinear.R
\name{timeDelayEmbed}
\alias{timeDelayEmbed}
\title{Time-delay phase-space embedding}
\usage{
timeDelayEmbed(
  x,
  delay = NULL,
  dim = NULL,
  max_delay = 50L,
  max_dim = 10L,
  n_bins = 16L,
  fnn_threshold = 0.01
)
}
\arguments{
\item{x}{Numeric time series.}

\item{delay}{Embedding delay in samples; \code{NULL} to estimate via AMI.}

\item{dim}{Embedding dimension; \code{NULL} to estimate via FNN.}

\item{max_delay}{Maximum delay searched for the AMI minimum.}

\item{max_dim}{Maximum embedding dimension searched for FNN.}

\item{n_bins}{Histogram bins for the AMI estimate.}

\item{fnn_threshold}{FNN fraction below which the dimension is accepted.}
}
\value{
A \code{time_delay_embedding} list with \code{embedded} (matrix, points x dim),
\code{delay}, \code{dim}, \code{ami} (if estimated) and \code{fnn} (if estimated).
}
\description{
Reconstructs a scalar time series into a delay-embedded phase space
\eqn{Y(i) = [x_i, x_{i+\tau}, \dots, x_{i+(m-1)\tau}]}. When \code{delay} is \code{NULL}
it is chosen at the first local minimum of the average mutual information
(AMI); when \code{dim} is \code{NULL} it is chosen where the fraction of false nearest
neighbours (FNN) first drops below \code{fnn_threshold}.
}
\examples{
x <- sin(seq(0, 20 * pi, length.out = 500))
emb <- timeDelayEmbed(x, delay = 5, dim = 3)
dim(emb$embedded)
}
\references{
Fraser AM, Swinney HL (1986); Kennel MB, et al. (1992).
}
\seealso{
\code{\link[=maxLyapunovExponent]{maxLyapunovExponent()}}, \code{\link[=sampleEntropy]{sampleEntropy()}}
}
