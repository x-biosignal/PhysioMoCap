% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-forceplate.R
\name{computeImpulse}
\alias{computeImpulse}
\title{Compute vertical impulse from GRF}
\usage{
computeImpulse(
  force,
  sampling_rate,
  threshold = 20,
  subtract_threshold = FALSE
)
}
\arguments{
\item{force}{Numeric vector or matrix of vertical force values.}

\item{sampling_rate}{Sampling rate in Hz.}

\item{threshold}{Contact threshold in force units.}

\item{subtract_threshold}{Logical; if \code{TRUE}, subtracts \code{threshold} before
integrating each stance (negative values clipped to zero).}
}
\value{
For vector input: data.frame with one row per stance and columns
including \code{impulse} (force*time units).
For matrix input: same structure plus \code{channel}.
}
\description{
Integrates vertical force over detected stance phases using trapezoidal
integration.
}
\examples{
grf <- c(rep(0, 50), rep(600, 100), rep(0, 50))
computeImpulse(grf, sampling_rate = 1000)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=analyzeForcePlate]{analyzeForcePlate()}} for comprehensive force plate analysis,
\code{\link[=computeLoadingRate]{computeLoadingRate()}} for loading rate computation.
}
