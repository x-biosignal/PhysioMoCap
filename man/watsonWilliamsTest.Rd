% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-circular.R
\name{watsonWilliamsTest}
\alias{watsonWilliamsTest}
\title{Watson-Williams test for equal mean directions}
\usage{
watsonWilliamsTest(angles, group = NULL, units = c("degrees", "radians"))
}
\arguments{
\item{angles}{Numeric vector of angles, or a list of per-group angle vectors.}

\item{group}{Grouping factor (length \code{length(angles)}); required when \code{angles}
is a single vector, ignored when \code{angles} is a list.}

\item{units}{\code{"degrees"} (default) or \code{"radians"}.}
}
\value{
an \code{htest}: \code{F} statistic (concentration-corrected), df and \code{p.value}.
Warns when the pooled concentration is low (\code{rbar < 0.45}), where the test
is unreliable.
}
\description{
The circular analogue of a one-way ANOVA: tests whether two or more groups of
angles share a common mean direction (assuming von Mises data of similar
concentration). Use it to compare, e.g., coupling angles between limbs, speeds
or groups.
}
\examples{
set.seed(2)
g1 <- rnorm(25, 20, 10); g2 <- rnorm(25, 60, 10)
watsonWilliamsTest(list(g1, g2))$p.value              # differ -> small
}
\references{
Watson GS, Williams EJ (1956); Zar JH (1999) eq. 27.9.
}
\seealso{
\code{\link[=rayleighTest]{rayleighTest()}}
}
