% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability-orbital-rqa.R
\name{approximateEntropy}
\alias{approximateEntropy}
\title{Approximate entropy (Pincus)}
\usage{
approximateEntropy(x, m = 2L, r = NULL)
}
\arguments{
\item{x}{Numeric time series.}

\item{m}{Pattern length (default 2).}

\item{r}{Tolerance; if \code{NULL}, \code{0.2 * sd(x)}.}
}
\value{
the approximate entropy (scalar).
}
\description{
The regularity of a time series: the (negative) log-likelihood that patterns
close for \code{m} samples stay close for \code{m + 1}. Lower = more regular/predictable,
higher = more irregular. The ApEn complement to the package's sample entropy
(ApEn includes self-matches and is more biased for short series, but is the
classical measure).
}
\examples{
t <- seq(0, 20 * pi, length.out = 500)
approximateEntropy(sin(t))                          # low (regular)
set.seed(1); approximateEntropy(rnorm(500))         # high (irregular)
}
\references{
Pincus SM (1991) PNAS 88:2297-2301.
}
\seealso{
\code{\link[=recurrenceQuantification]{recurrenceQuantification()}}, \code{\link[=sampleEntropy]{sampleEntropy()}}
}
