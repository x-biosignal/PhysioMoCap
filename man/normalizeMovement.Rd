% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-normalize.R
\name{normalizeMovement}
\alias{normalizeMovement}
\title{Normalize movement data}
\usage{
normalizeMovement(
  x,
  method = c("cycle", "phase", "landmark", "dtw", "absolute"),
  norm_length = 101L,
  schema = NULL,
  events = NULL,
  reference = NULL,
  ...
)
}
\arguments{
\item{x}{Data to normalize:
\itemize{
\item PhysioExperiment object
\item Matrix (time x channels)
\item segmented_phases object
\item List of matrices (multiple trials)
}}

\item{method}{Normalization method:
\itemize{
\item "cycle" - Normalize entire movement to fixed length (0-100\%)
\item "phase" - Normalize each phase independently
\item "landmark" - Align to a specific event
\item "dtw" - Dynamic time warping alignment
\item "absolute" - Keep original time (no normalization)
}}

\item{norm_length}{Target length after normalization (default 101 for 0-100\%)}

\item{schema}{Optional TaskSchema for context}

\item{events}{Optional detected_events for landmark alignment}

\item{reference}{Reference trial for DTW (default: mean of all trials)}

\item{...}{Additional arguments passed to specific methods}
}
\value{
Normalized data in the same format as input (or matrix for
segmented_phases)
}
\description{
Normalizes movement data using various methods appropriate for different
task types.
}
\examples{
# Normalize a matrix to 101 points
data <- matrix(rnorm(500), nrow = 500, ncol = 3)
normalized <- normalizeMovement(data, method = "cycle", norm_length = 101)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. Wiley.
}
\seealso{
\code{\link[=batchNormalize]{batchNormalize()}}, \code{\link[=normalizedTimeAxis]{normalizedTimeAxis()}}, \code{\link[=segmentPhases]{segmentPhases()}}
}
