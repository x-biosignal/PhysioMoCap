% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posturography.R
\name{stabilogramDiffusion}
\alias{stabilogramDiffusion}
\title{Stabilogram diffusion analysis (Collins & De Luca 1993)}
\usage{
stabilogramDiffusion(
  cop,
  sampling_rate,
  ap = NULL,
  ml = NULL,
  detrend = c("mean", "none"),
  max_interval = 10,
  short_max = 1,
  long_min = 2.5
)
}
\arguments{
\item{cop, ap, ml, sampling_rate, detrend}{As in \code{\link[=swayMetrics]{swayMetrics()}}.}

\item{max_interval}{Maximum time interval in seconds over which to compute the
MSD (default 10, capped at half the record length).}

\item{short_max, long_min}{Interval boundaries (seconds) for the short-term
(default \verb{<= 1 s}) and long-term (default \verb{>= 2.5 s}) linear regions used to
estimate the diffusion coefficients.}
}
\value{
A \code{stabilogram_diffusion} object with, per component (\code{planar}, \code{ap},
\code{ml}), the short/long diffusion coefficients, scaling exponents (Hurst) and
the critical-point interval and MSD.
}
\description{
Computes the mean square displacement (MSD) of the CoP as a function of the
time interval and extracts the short-term and long-term diffusion
coefficients and the critical point (the crossover between open-loop and
closed-loop postural control). Planar (resultant) and per-axis analyses are
returned.
}
\examples{
set.seed(1)
n <- 3000
cop <- data.frame(cop_x = cumsum(rnorm(n)) * 0.05,
                  cop_y = cumsum(rnorm(n)) * 0.05)
stabilogramDiffusion(cop, sampling_rate = 100)
}
\references{
Collins JJ, De Luca CJ (1993). Exp Brain Res 95:308-318.
}
\seealso{
\code{\link[=swayMetrics]{swayMetrics()}}
}
