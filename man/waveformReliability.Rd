% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-reliability.R
\name{waveformReliability}
\alias{waveformReliability}
\title{Waveform reliability report: CMC, ICC / SEM / MDC curves}
\usage{
waveformReliability(x, model = "twoway", type = "agreement", confidence = 0.95)
}
\arguments{
\item{x}{A \verb{[subject, trial, frame]} array, or a list of \verb{trials x frames}
matrices (one per subject).}

\item{model, type}{Passed to \code{\link[PhysioCore:icc]{PhysioCore::icc()}} for the pointwise ICC.}

\item{confidence}{Confidence level for the MDC (default 0.95).}
}
\value{
a \code{waveform_reliability} object with per-subject \code{cmc}, the \code{icc},
\code{sem}, \code{mdc} curves (+ ICC CIs), and scalar summaries (\code{mean_cmc},
\code{mean_icc}, \code{peak_mdc}, ...). The SEM curve is the between-subject SD at each
frame times sqrt(1 - ICC); the MDC curve is the 95\% minimal detectable
change in the measurement's own units.
}
\description{
The integrated movement-waveform reliability summary. It combines the
within-subject repeatability (per-subject Kadaba \code{\link[=waveformCMC]{waveformCMC()}}) with the
between-subject pointwise ICC curve and the frame-by-frame standard error of
measurement and minimal detectable change (from \code{\link[PhysioCore:sem]{PhysioCore::sem()}} /
\code{\link[PhysioCore:mdc]{PhysioCore::mdc()}}, the same definitions as the scalar clinimetrics).
}
\examples{
set.seed(1)
subs <- lapply(1:10, function(s) {
  base <- s + 5 * sin(seq(0, 2 * pi, length.out = 101))
  t(sapply(1:3, function(k) base + rnorm(101, 0, 0.3)))
})
r <- waveformReliability(subs)
r$mean_cmc; r$mean_icc; r$peak_mdc
}
\references{
Kadaba MP, et al. (1989) J Orthop Res 7:849-860; Weir JP (2005)
J Strength Cond Res 19:231-240 (SEM/MDC).
}
\seealso{
\code{\link[=waveformCMC]{waveformCMC()}}, \code{\link[=waveformICC]{waveformICC()}}
}
