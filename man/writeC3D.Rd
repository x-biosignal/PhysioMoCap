% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-c3d.R
\name{writeC3D}
\alias{writeC3D}
\title{Write a C3D Motion Capture File}
\usage{
writeC3D(x, path, include_analog = TRUE)
}
\arguments{
\item{x}{A \code{PhysioExperiment} with \code{"position_x"}, \code{"position_y"}, and
\code{"position_z"} assays (as produced by \code{\link[=readC3D]{readC3D()}} or \code{\link[=readTRC]{readTRC()}}), or a
\code{MultiRatePhysioExperiment} whose marker stream carries those assays.}

\item{path}{Character string giving the output \code{.c3d} path.}

\item{include_analog}{Logical; if \code{TRUE} (default) and the object carries
analog data (in \code{metadata(x)$analog_data}, or as an \code{"analog"} stream of
a \code{MultiRatePhysioExperiment}), that data is written as C3D analog
channels. If \code{FALSE}, a marker-only C3D file is written.}
}
\value{
The output \code{path}, invisibly.
}
\description{
Writes 3D marker trajectories (and optional analog channels) from a
\code{PhysioExperiment} to a binary C3D file via the \pkg{c3dr} package. This
is the inverse of \code{\link[=readC3D]{readC3D()}}: a \code{readC3D() -> writeC3D() -> readC3D()}
round-trip reproduces the marker coordinates (within 32-bit float
precision) and preserves the point/analog sampling-rate ratio.
}
\details{
The point (marker) rate is taken from \code{samplingRate(x)}. When analog data
is written, the analog rate and the integer point/analog ratio
(\code{ANALOG:RATE / POINT:RATE}, i.e. analog subframes per point frame) are
derived from the analog block so the ratio round-trips exactly. Force
platform metadata is not written (the marker and analog signals are
preserved; force-plate corner/calibration parameters are dropped).
}
\examples{
if (requireNamespace("c3dr", quietly = TRUE)) {
  pe <- readC3D(c3dr::c3d_example(), include_analog = TRUE)
  out <- tempfile(fileext = ".c3d")
  writeC3D(pe, out)
  pe2 <- readC3D(out)
}
}
\references{
C3D.org. "The C3D File Format." \url{https://www.c3d.org/}.
}
\seealso{
\code{\link[=readC3D]{readC3D()}}, \code{\link[=writeTRC]{writeTRC()}}, \code{\link[=writeMOT]{writeMOT()}}
}
