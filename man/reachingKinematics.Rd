% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reaching.R
\name{reachingKinematics}
\alias{reachingKinematics}
\title{Reaching kinematics report for a PhysioExperiment}
\usage{
reachingKinematics(
  pe,
  marker,
  target = NULL,
  assay_prefix = "position",
  sampling_rate = NULL,
  onset_threshold = 0.05,
  trunk_marker = NULL,
  shoulder_markers = NULL,
  ...
)
}
\arguments{
\item{pe}{A \code{PhysioExperiment} with \code{position_x}, \code{position_y}, and
optionally \code{position_z} assays.}

\item{marker}{Reaching hand marker name or column index.}

\item{target}{Optional target coordinate matching trajectory dimension.}

\item{assay_prefix}{Position-assay prefix (default \code{"position"}).}

\item{sampling_rate}{Optional sampling frequency in Hz; defaults to
\code{\link[PhysioCore:samplingRate]{PhysioCore::samplingRate()}}.}

\item{onset_threshold}{Movement threshold.}

\item{trunk_marker}{Optional trunk marker name or column index.}

\item{shoulder_markers}{Optional length-2 vector identifying right and left
shoulder markers.}

\item{...}{Additional arguments passed to \code{\link[=sparc]{sparc()}}.}
}
\value{
A \code{reaching_kinematics} report with movement time, peak velocity,
time to peak, movement units, SPARC, LDLJ, dimensionless jerk, movement
bounds, sampling rate, marker, and optional endpoint/trunk results.
}
\description{
Extracts a hand-marker trajectory, computes tangential speed with
\code{\link[=computeVelocity]{computeVelocity()}} and \code{\link[=computeSpeed]{computeSpeed()}}, and reports temporal, submovement,
smoothness, endpoint, and optional trunk-compensation metrics.
}
\examples{
\dontrun{
pe <- make_mocap_markers(n_time = 200, n_markers = 3, sr = 200)
reachingKinematics(pe, marker = "Marker1")
}
}
