% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ataxia.R
\name{limbAtaxiaIndex}
\alias{limbAtaxiaIndex}
\title{Limb-ataxia composite from reaching sub-metrics}
\usage{
limbAtaxiaIndex(
  dysmetria = NA_real_,
  decomposition = NA_real_,
  smoothness = NA_real_,
  irregularity = NA_real_,
  reference = NULL,
  weights = NULL
)
}
\arguments{
\item{dysmetria, decomposition, smoothness, irregularity}{Numeric sub-metrics
(higher = more ataxic); any may be \code{NA} to omit it.}

\item{reference}{Optional data.frame of the same-named columns measured in a
healthy reference sample; when given, each sub-metric is z-standardised
against it and averaged into \code{composite}. Without a reference the composite
is \code{NA} and only the sub-metrics are returned.}

\item{weights}{Optional named weights for the composite (default equal).}
}
\value{
An S3 \code{limb_ataxia} list: \code{metrics}, per-metric \code{z}, and \code{composite}.
}
\description{
Aggregates the standard limb-ataxia sub-metrics, each oriented so that a
higher value means more ataxic movement. Supply them from the existing
primitives: \code{dysmetria} from \code{\link[=endpointError]{endpointError()}} (\code{absolute_error} or the
magnitude of \code{constant_error}), \code{decomposition} from \code{\link[=movementUnits]{movementUnits()}} (the
submovement count), \code{smoothness} as a jerk/roughness score where higher is
worse (e.g. \code{-sparc} from \code{\link[=sparc]{sparc()}}, since SPARC is more negative for less
smooth movement, or \code{ldlj} magnitude), and \code{irregularity} from
\code{\link[=pathStraightness]{pathStraightness()}} (\code{index_of_curvature}).
}
\examples{
limbAtaxiaIndex(dysmetria = 3.2, decomposition = 4, smoothness = 5.1,
                irregularity = 1.4)
}
\seealso{
\code{\link[=pathStraightness]{pathStraightness()}}, \code{\link[=reachingKinematics]{reachingKinematics()}}, \code{\link[=gaitAtaxiaIndex]{gaitAtaxiaIndex()}}
}
