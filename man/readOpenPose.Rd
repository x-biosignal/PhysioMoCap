% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-openpose.R
\name{readOpenPose}
\alias{readOpenPose}
\title{Read OpenPose JSON output}
\usage{
readOpenPose(path, model = c("BODY_25", "COCO"), fps = 30, person_id = 1L)
}
\arguments{
\item{path}{Path to a directory of JSON files or a single JSON file.}

\item{model}{Keypoint model: \code{"BODY_25"} (25 keypoints) or \code{"COCO"}
(18 keypoints). Default \code{"BODY_25"}.}

\item{fps}{Frame rate in Hz (frames per second). Default 30.}

\item{person_id}{Which person to extract (1-based index). Default 1
(first detected person).}
}
\value{
A PhysioExperiment with assays:
\describe{
\item{keypoint_x}{X coordinates matrix (frames x keypoints)}
\item{keypoint_y}{Y coordinates matrix (frames x keypoints)}
\item{confidence}{Detection confidence matrix (frames x keypoints)}
}

The \code{colData} contains columns \code{label} (keypoint name), \code{type}
(\code{"keypoint"}), and \code{model} (the OpenPose model used).
}
\description{
Reads OpenPose JSON keypoint data from a directory of frame files
or a single JSON file and returns a PhysioExperiment object.
}
\details{
OpenPose outputs one JSON file per video frame. Each file contains a
\code{"people"} array where each person's pose is stored as a flat array
of \verb{[x, y, confidence]} triplets.

\strong{BODY_25 model} (25 keypoints): Nose, Neck, RShoulder, RElbow,
RWrist, LShoulder, LElbow, LWrist, MidHip, RHip, RKnee, RAnkle,
LHip, LKnee, LAnkle, REye, LEye, REar, LEar, LBigToe, LSmallToe,
LHeel, RBigToe, RSmallToe, RHeel.

\strong{COCO model} (18 keypoints): Nose, Neck, RShoulder, RElbow,
RWrist, LShoulder, LElbow, LWrist, RHip, RKnee, RAnkle, LHip,
LKnee, LAnkle, REye, LEye, REar, LEar.

Frames where the specified person is not detected will contain
\code{NA} values for all keypoints.
}
\examples{
\dontrun{
# Read directory of OpenPose JSON files
pe <- readOpenPose("path/to/openpose_output/", fps = 30)

# Read with COCO model
pe <- readOpenPose("path/to/output/", model = "COCO", fps = 25)

# Extract second person
pe <- readOpenPose("path/to/output/", person_id = 2)
}

}
\references{
Cao Z, Hidalgo G, Simon T, Wei SE, Sheikh Y (2019). "OpenPose:
Realtime Multi-Person 2D Pose Estimation Using Part Affinity Fields."
IEEE Transactions on Pattern Analysis and Machine Intelligence,
43(1), 172-186.
}
\seealso{
\code{\link[=readDeepLabCut]{readDeepLabCut()}}, \code{\link[=readMediaPipe]{readMediaPipe()}}, \code{\link[=define_skeleton]{define_skeleton()}}
}
