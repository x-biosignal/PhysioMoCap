% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spasticity-tardieu.R
\name{tardieuStretch}
\alias{tardieuStretch}
\title{Detect the spastic catch on a single passive stretch}
\usage{
tardieuStretch(
  angle,
  emg = NULL,
  sampling_rate,
  emg_sampling_rate = sampling_rate,
  onset = c("emg", "velocity", "both"),
  baseline_sec = 0.5,
  threshold_sd = 3,
  catch_fraction = 0.5,
  envelope_ms = 50
)
}
\arguments{
\item{angle}{Numeric joint-angle trace over the stretch (degrees; increasing
as the joint is stretched).}

\item{emg}{Optional EMG of the stretched muscle (numeric); enables the EMG
reflex-onset trigger.}

\item{sampling_rate}{Angle sampling rate (Hz).}

\item{emg_sampling_rate}{EMG sampling rate (Hz); the EMG is resampled onto the
angle time base with \code{\link[=alignEMGtoMoCap]{alignEMGtoMoCap()}}. Default = \code{sampling_rate}.}

\item{onset}{Catch trigger: \code{"emg"}, \code{"velocity"} or \code{"both"} (EMG-preferred,
velocity fallback).}

\item{baseline_sec}{Baseline window (s) for the EMG threshold (default 0.5).}

\item{threshold_sd}{EMG onset threshold in baseline SDs (default 3).}

\item{catch_fraction}{Velocity-arrest fraction of peak velocity (default 0.5).}

\item{envelope_ms}{EMG RMS-envelope window in ms (default 50).}
}
\value{
An S3 \code{tardieu_stretch} list: \code{catch_angle} (R1), \code{catch_index},
\code{catch_velocity}, \code{peak_velocity}, \code{rom_max}/\code{rom_min}, \code{reflex_latency_ms}
(EMG onset relative to stretch onset), \code{onset_method}.
}
\description{
Locates the Tardieu "catch" on one passive-stretch joint-angle trace and
returns the catch angle (R1). The catch is detected from a reflex EMG burst
(envelope crossing a baseline mean + \code{threshold_sd} * SD threshold), from a
velocity arrest (angular velocity dropping below \code{catch_fraction} of its peak
after the fastest stretch), or from both.
}
\examples{
fs <- 200; t <- seq(0, 0.6, by = 1 / fs)
angle <- ifelse(t <= 0.25, 25 * t / 0.25, 25 + 5 * (t - 0.25) / 0.35)
tardieuStretch(angle, sampling_rate = fs, onset = "velocity")$catch_angle
}
\seealso{
\code{\link[=tardieuScore]{tardieuScore()}}, \code{\link[=reflexThreshold]{reflexThreshold()}}
}
