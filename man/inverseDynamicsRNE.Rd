% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-rne.R
\name{inverseDynamicsRNE}
\alias{inverseDynamicsRNE}
\title{Recursive Newton-Euler inverse dynamics for the lower limb}
\usage{
inverseDynamicsRNE(
  joints,
  grf,
  inertia,
  sampling_rate,
  dims = c("2D", "3D"),
  gravity = .GRAVITY,
  vertical = c("y", "z")
)
}
\arguments{
\item{joints}{A matrix/data.frame of joint-centre coordinates over time with
columns \code{ankle_x}, \code{ankle_y}, \code{knee_x}, \code{knee_y}, \code{hip_x}, \code{hip_y} and the
foot distal end \code{toe_x}, \code{toe_y} (for \code{dims = "3D"} add the \verb{_z} columns).}

\item{grf}{A matrix/data.frame with the ground reaction force \code{fx}, \code{fy}
(and \code{fz} for 3D), the centre of pressure \code{cop_x}, \code{cop_y} (and \code{cop_z}),
and an optional free moment: \code{tz} in 2D, or any of \code{tx}/\code{ty}/\code{tz} in 3D
(each defaulting to 0).}

\item{inertia}{A segment inertia table as returned by
\code{\link[=estimateSegmentInertia]{estimateSegmentInertia()}} (rows \code{foot}, \code{shank}, \code{thigh}).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{dims}{\code{"2D"} (sagittal plane, default) or \code{"3D"}.}

\item{gravity}{Gravitational acceleration (default 9.80665 m/s^2).}

\item{vertical}{Which coordinate axis points up: \code{"y"} (the default, matching
the sagittal x-y convention) or \code{"z"}. Only meaningful for \code{dims = "3D"};
a 2D chain must use \code{"y"}. Gravity acts along the negative of this axis, so
getting it wrong silently mis-signs every gravitational moment.}
}
\value{
A data.frame with \code{time}, the net joint moments
(\code{ankle_moment}, \code{knee_moment}, \code{hip_moment}) and the proximal joint
reaction forces for each joint.
}
\description{
Computes net ankle, knee and hip joint moments (and the joint reaction
forces) with a proper distal-to-proximal recursive Newton-Euler chain
(foot -> shank -> thigh). The ground reaction force and free moment enter at
the foot; each segment contributes its inertia and gravity. Segment linear
and angular accelerations are obtained by numerical differentiation of the
marker-derived centre-of-mass and orientation trajectories.
}
\examples{
n <- 50
joints <- data.frame(
  ankle_x = rep(0, n), ankle_y = rep(0.08, n),
  toe_x = rep(0.15, n), toe_y = rep(0.03, n),
  knee_x = rep(0, n),   knee_y = rep(0.48, n),
  hip_x = rep(0, n),    hip_y = rep(0.90, n))
grf <- data.frame(fx = rep(0, n), fy = rep(0, n),
                  cop_x = rep(0, n), cop_y = rep(0, n))
inertia <- estimateSegmentInertia(body_mass = 70, body_height = 1.75)
inverseDynamicsRNE(joints, grf, inertia, sampling_rate = 100)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. Wiley. Featherstone R (2008). "Rigid Body Dynamics Algorithms."
}
\seealso{
\code{\link[=estimateSegmentInertia]{estimateSegmentInertia()}}, \code{\link[=scaleBodyModel]{scaleBodyModel()}}, \code{\link[=inverseDynamics2D]{inverseDynamics2D()}}
}
