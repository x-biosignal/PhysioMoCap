% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/movement-tremor.R
\name{tremorSpectrum}
\alias{tremorSpectrum}
\title{Tremor power spectrum and dominant frequency}
\usage{
tremorSpectrum(
  signal,
  sampling_rate,
  band = c(3, 12),
  detrend = TRUE,
  seg_sec = NULL
)
}
\arguments{
\item{signal}{Numeric vector: one axis, or the vector magnitude, of an
accelerometer / gyroscope / displacement signal (gravity removed for raw
accelerometry).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{band}{Length-2 tremor band \code{c(low, high)} in Hz (default \code{c(3, 12)}).}

\item{detrend}{Remove the signal mean before spectral estimation (default
\code{TRUE}; each Welch segment is also demeaned).}

\item{seg_sec}{Optional Welch segment length in seconds (default: about an
eighth of the record, rounded to a power of two).}
}
\value{
A list: \code{dominant_freq_hz}, \code{peak_power}, \code{band_power_abs},
\code{band_power_rel} (in-band / total power), \code{half_power_bw_hz},
\code{harmonic_ratio} (power at 2*f0 / power at f0), and the \code{freq} / \code{psd}
vectors.
}
\description{
Estimates the power spectrum of a movement signal (Welch periodogram) and
summarises the tremor within a frequency band: the dominant frequency, its
peak power, absolute and relative in-band power, the half-power bandwidth
(peak sharpness) and the first-harmonic ratio.
}
\examples{
fs <- 100; t <- seq(0, 20, by = 1 / fs)
x <- 2 * sin(2 * pi * 5 * t)            # a 5 Hz, amplitude-2 tremor
tremorSpectrum(x, fs)$dominant_freq_hz
}
\seealso{
\code{\link[=tremorAmplitude]{tremorAmplitude()}}, \code{\link[=tremorMetrics]{tremorMetrics()}}
}
