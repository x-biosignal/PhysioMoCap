% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mocap-skeleton.R
\name{SkeletonModel}
\alias{SkeletonModel}
\title{Create a SkeletonModel object}
\usage{
SkeletonModel(name, keypoints, bones, hierarchy, root_keypoint)
}
\arguments{
\item{name}{Character string identifying the skeleton model
(e.g., \code{"BODY_25"}, \code{"COCO"}).}

\item{keypoints}{A \code{data.frame} with columns:
\describe{
\item{id}{Integer, 0-indexed keypoint identifier.}
\item{label}{Character, human-readable keypoint name.}
\item{body_region}{Character, anatomical region
(e.g., \code{"head"}, \code{"torso"}, \code{"left_arm"}).}
}}

\item{bones}{A \code{data.frame} with columns:
\describe{
\item{from_id}{Integer, source keypoint id.}
\item{to_id}{Integer, target keypoint id.}
\item{bone_name}{Character, descriptive name for the bone segment.}
}}

\item{hierarchy}{Named list of parent-to-children relationships. Each
element name is a parent keypoint label; the value is a character
vector of child keypoint labels.}

\item{root_keypoint}{Character, label of the root keypoint in the
hierarchy (e.g., \code{"MidHip"} for BODY_25).}
}
\value{
A \code{SkeletonModel} object (S3 class).
}
\description{
Constructs an S3 \code{SkeletonModel} object that defines a skeleton topology
for pose estimation or motion capture data. A skeleton consists of named
keypoints (joints/markers), bones connecting them, and an optional
hierarchical tree structure.
}
\examples{
# Minimal skeleton
kp <- data.frame(
  id = 0:2,
  label = c("Head", "Torso", "Hip"),
  body_region = c("head", "torso", "pelvis")
)
bones <- data.frame(
  from_id = c(0, 1),
  to_id = c(1, 2),
  bone_name = c("neck", "spine")
)
hier <- list(Head = "Torso", Torso = "Hip")
sk <- SkeletonModel("mini", kp, bones, hier, "Head")
print(sk)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. Wiley.
}
\seealso{
\code{\link[=define_skeleton]{define_skeleton()}}, \code{\link[=get_bone_connections]{get_bone_connections()}}, \code{\link[=get_segment_lengths]{get_segment_lengths()}}
}
