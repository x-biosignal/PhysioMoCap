% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-csv.R
\name{readMoCapCSV}
\alias{readMoCapCSV}
\title{Read Motion Capture CSV Data}
\usage{
readMoCapCSV(
  path,
  format = c("auto", "xyz", "wide", "long", "qualisys", "vicon"),
  sampling_rate = NULL,
  header_rows = 1L,
  skip = 0L,
  sep = ",",
  marker_names = NULL,
  coord_columns = NULL
)
}
\arguments{
\item{path}{Path to the CSV file.}

\item{format}{Format hint: \code{"auto"} (detect), \code{"xyz"} (columns like
\code{marker1_x}, \code{marker1_y}, \code{marker1_z}), \code{"wide"} (columns like
\code{Time}, \code{M1X}, \code{M1Y}, \code{M1Z}), \code{"long"} (columns: \code{frame}, \code{marker},
\code{x}, \code{y}, \code{z}), \code{"qualisys"} (Qualisys TSV export), or \code{"vicon"}
(Vicon CSV export). Default \code{"auto"}.}

\item{sampling_rate}{Sampling rate in Hz. Required if not detectable from
the file (e.g., from a Time column). If \code{NULL} and not detectable, an
error is raised.}

\item{header_rows}{Number of header rows. Default 1.}

\item{skip}{Number of lines to skip before reading. Default 0.}

\item{sep}{Column separator. Default \code{","}.}

\item{marker_names}{Explicit marker names (overrides auto-detection from
column names). Must match the number of markers detected from columns.}

\item{coord_columns}{Mapping of coordinate types as a named list of regex
patterns, e.g., \code{list(x = "_X$", y = "_Y$", z = "_Z$")}. Used only
when \code{format} is \code{"wide"} or \code{"auto"}.}
}
\value{
A PhysioExperiment with assays:
\describe{
\item{position_x}{X coordinates matrix (frames x markers)}
\item{position_y}{Y coordinates matrix (frames x markers)}
\item{position_z}{Z coordinates matrix (frames x markers)}
}

The \code{colData} contains columns \code{label} (marker names) and \code{type}
(\code{"marker"}). The \code{metadata} list contains \code{format}, \code{source_file},
and optionally \code{time} (if a Time column was found).
}
\description{
Generic CSV reader that handles common motion capture CSV exports from
various systems including Qualisys, Vicon, and generic marker position
formats. Returns a PhysioExperiment with \code{position_x}, \code{position_y}, and
\code{position_z} assays.
}
\details{
\subsection{Auto-detection logic}{

When \code{format = "auto"}, the function inspects column names:
\enumerate{
\item If columns match \verb{*_x}, \verb{*_y}, \verb{*_z} pattern (case-insensitive)
with consistent marker prefixes, the \code{"xyz"} format is used.
\item If columns match \verb{*X}, \verb{*Y}, \verb{*Z} suffix pattern, the \code{"wide"}
format is used.
\item If the header contains "Qualisys" or "QTM", the \code{"qualisys"}
format is used.
\item If columns include \code{frame}, \code{marker}, \code{x}, \code{y}, \code{z}
(case-insensitive), the \code{"long"} format is used.
}

If a \code{Time} or \code{time} column is present and contains numeric values,
the sampling rate is computed from the median time step. An explicit
\code{sampling_rate} argument always takes precedence.
}
}
\examples{
\dontrun{
# Read xyz-format CSV
pe <- readMoCapCSV("markers.csv", sampling_rate = 120)

# Read with auto-detection from Time column
pe <- readMoCapCSV("markers.csv")

# Read tab-separated file
pe <- readMoCapCSV("qualisys_export.tsv", format = "qualisys", sep = "\t")

# Override marker names
pe <- readMoCapCSV("data.csv", marker_names = c("Hip", "Knee", "Ankle"),
                   sampling_rate = 100)
}

}
\references{
Wickham H (2014). "Tidy Data." Journal of Statistical Software, 59(10), 1-23.
}
\seealso{
\code{\link[=readASF]{readASF()}} and \code{\link[=readAMC]{readAMC()}} for Acclaim skeleton and motion files,
\code{\link[=readMoCapAuto]{readMoCapAuto()}} for automatic format detection.
}
