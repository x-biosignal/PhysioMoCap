% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kinetics-forceplate.R
\name{filterGRF}
\alias{filterGRF}
\title{Low-pass filter ground reaction force data}
\usage{
filterGRF(
  x,
  sampling_rate,
  cutoff = 20,
  order = 4,
  method = c("butterworth", "moving_average")
)
}
\arguments{
\item{x}{Numeric vector or matrix (time x channels).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{cutoff}{Low-pass cutoff frequency in Hz.}

\item{order}{Butterworth filter order.}

\item{method}{Filtering method: \code{"butterworth"} or \code{"moving_average"}.}
}
\value{
Filtered data with the same dimensions as \code{x}.
}
\description{
Applies low-pass filtering to force-plate signals. By default this uses a
zero-phase Butterworth filter (requires the \code{signal} package). If \code{signal}
is not available, the function falls back to a moving-average filter.
}
\examples{
sr <- 1000
t <- seq(0, 1, length.out = sr)
x <- sin(2 * pi * 10 * t) + 0.2 * sin(2 * pi * 150 * t)
y <- filterGRF(x, sampling_rate = sr, cutoff = 25)
}
\references{
Winter DA (2009). "Biomechanics and Motor Control of Human Movement."
4th ed. John Wiley & Sons.
}
\seealso{
\code{\link[=butterworthFilter]{butterworthFilter()}} for general Butterworth filtering,
\code{\link[=analyzeForcePlate]{analyzeForcePlate()}} for comprehensive force plate analysis.
}
