% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-reliability.R
\name{waveformCMC}
\alias{waveformCMC}
\title{Coefficient of multiple correlation for repeated waveforms (Kadaba CMC)}
\usage{
waveformCMC(x, groups = NULL)
}
\arguments{
\item{x}{A \verb{trials x frames} numeric matrix: each row one waveform (e.g. a
joint angle over the time-normalised cycle), columns the cycle points.}

\item{groups}{Optional grouping (length \code{nrow(x)}); when given, the CMC is
computed between the group-mean waveforms (between-day/-condition).}
}
\value{
The CMC in [0, 1] (higher = more repeatable), or \code{NA} with a warning
when the radicand is negative (waveform variation too small for a defined
CMC -- a known limitation for low-excursion curves).
}
\description{
The waveform analogue of a reliability coefficient (Kadaba et al. 1989): how
similar a set of repeated curves are, relative to the curve's own variation.
With \code{groups = NULL} the rows of \code{x} are repeated trials and the result is the
\strong{within-day} repeatability; pass \code{groups} (e.g. a session/day label per row)
for the \strong{between-day} CMC of the session-mean waveforms.
}
\examples{
base <- sin(seq(0, 2 * pi, length.out = 101))
trials <- t(sapply(1:5, function(i) base + rnorm(101, 0, 0.02)))
waveformCMC(trials)                                  # ~ 1 (repeatable)
}
\references{
Kadaba MP, et al. (1989) J Orthop Res 7:849-860.
}
\seealso{
\code{\link[=waveformICC]{waveformICC()}}, \code{\link[=waveformReliability]{waveformReliability()}}
}
