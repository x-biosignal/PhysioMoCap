# Create gait schema based on available OpenSim data

Create gait schema based on available OpenSim data

## Usage

``` r
.create_gait_schema_opensim(
  has_grf,
  has_heel_marker,
  has_toe_marker,
  has_kinematics,
  available_signals,
  side
)
```
