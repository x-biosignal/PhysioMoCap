# Parse key=value pairs from an OpenSim header block

Parse key=value pairs from an OpenSim header block

## Usage

``` r
.parse_opensim_header(header_lines)
```

## Arguments

- header_lines:

  Character vector of header lines (before endheader).

## Value

A named list of parsed header values.
