# Build an OpenCap API URL

Constructs a URL by joining the base URL with path segments.

## Usage

``` r
.opencap_build_url(base_url, ...)
```

## Arguments

- base_url:

  Character string with the base API URL.

- ...:

  Character path segments to append.

## Value

A single character string URL.
