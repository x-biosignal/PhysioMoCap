# Create running schema based on available OpenSim data

Create running schema based on available OpenSim data

## Usage

``` r
.create_running_schema_opensim(
  has_grf,
  has_kinematics,
  available_signals,
  side
)
```
